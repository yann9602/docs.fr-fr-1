---
title: "Ancres dans les expressions régulières"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-standard
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
helpviewer_keywords:
- atomic zero-width assertions
- regular expressions, anchors
- regular expressions, atomic zero-width assertions
- anchors, in regular expressions
- metacharacters, atomic zero-width assertions
- metacharacters, anchors
- .NET Framework regular expressions, anchors
- .NET Framework regular expressions, atomic zero-width assertions
ms.assetid: 336391f6-2614-499b-8b1b-07a6837108a7
caps.latest.revision: "20"
author: rpetrusha
ms.author: ronpet
manager: wpickett
ms.openlocfilehash: 453776c97ec0531cea94ecf44c31216cf5b17a3b
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: HT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/21/2017
---
# <a name="anchors-in-regular-expressions"></a><span data-ttu-id="e1a66-102">Ancres dans les expressions régulières</span><span class="sxs-lookup"><span data-stu-id="e1a66-102">Anchors in Regular Expressions</span></span>
<a name="top"></a> <span data-ttu-id="e1a66-103">Les ancres, ou assertions atomiques de largeur nulle, spécifient une position dans la chaîne où une correspondance doit se produire.</span><span class="sxs-lookup"><span data-stu-id="e1a66-103">Anchors, or atomic zero-width assertions, specify a position in the string where a match must occur.</span></span> <span data-ttu-id="e1a66-104">Quand vous utilisez une ancre dans votre expression de recherche, le moteur des expressions régulières n'avance pas dans la chaîne ou ne consomme pas de caractères ; il recherche uniquement une correspondance à la position spécifiée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-104">When you use an anchor in your search expression, the regular expression engine does not advance through the string or consume characters; it looks for a match in the specified position only.</span></span> <span data-ttu-id="e1a66-105">Par exemple, `^` spécifie que la correspondance doit commencer au début d'une ligne ou d'une chaîne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-105">For example, `^` specifies that the match must start at the beginning of a line or string.</span></span> <span data-ttu-id="e1a66-106">Par conséquent, l'expression régulière `^http:` correspond uniquement à « http: » quand elle se produit au début d'une ligne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-106">Therefore, the regular expression `^http:` matches "http:" only when it occurs at the beginning of a line.</span></span> <span data-ttu-id="e1a66-107">Le tableau suivant répertorie les ancres prises en charge par les expressions régulières dans .NET.</span><span class="sxs-lookup"><span data-stu-id="e1a66-107">The following table lists the anchors supported by the regular expressions in .NET.</span></span>  
  
|<span data-ttu-id="e1a66-108">Ancre</span><span class="sxs-lookup"><span data-stu-id="e1a66-108">Anchor</span></span>|<span data-ttu-id="e1a66-109">Description</span><span class="sxs-lookup"><span data-stu-id="e1a66-109">Description</span></span>|  
|------------|-----------------|  
|`^`|<span data-ttu-id="e1a66-110">La correspondance doit se produire au début de la chaîne ou de la ligne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-110">The match must occur at the beginning of the string or line.</span></span> <span data-ttu-id="e1a66-111">Pour plus d'informations, consultez [Début de chaîne ou de ligne](#Start).</span><span class="sxs-lookup"><span data-stu-id="e1a66-111">For more information, see [Start of String or Line](#Start).</span></span>|  
|`$`|<span data-ttu-id="e1a66-112">La correspondance doit se produire à la fin de la chaîne ou de la ligne ou avant `\n` à la fin de la chaîne ou de la ligne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-112">The match must occur at the end of the string or line, or before `\n` at the end of the string or line.</span></span> <span data-ttu-id="e1a66-113">Pour plus d'informations, consultez [Fin de chaîne ou de ligne](#End).</span><span class="sxs-lookup"><span data-stu-id="e1a66-113">For more information, see [End of String or Line](#End).</span></span>|  
|`\A`|<span data-ttu-id="e1a66-114">La correspondance doit se produire au début de la chaîne uniquement (aucun support multiligne).</span><span class="sxs-lookup"><span data-stu-id="e1a66-114">The match must occur at the beginning of the string only (no multiline support).</span></span> <span data-ttu-id="e1a66-115">Pour plus d'informations, consultez [Début de chaîne uniquement](#StartOnly).</span><span class="sxs-lookup"><span data-stu-id="e1a66-115">For more information, see [Start of String Only](#StartOnly).</span></span>|  
|`\Z`|<span data-ttu-id="e1a66-116">La correspondance doit se produire à la fin de la chaîne, ou avant `\n` à la fin de la chaîne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-116">The match must occur at the end of the string, or before `\n` at the end of the string.</span></span> <span data-ttu-id="e1a66-117">Pour plus d'informations, consultez [Fin de chaîne ou avant un saut de ligne final](#EndOrNOnly).</span><span class="sxs-lookup"><span data-stu-id="e1a66-117">For more information, see [End of String or Before Ending Newline](#EndOrNOnly).</span></span>|  
|`\z`|<span data-ttu-id="e1a66-118">La correspondance doit se produire uniquement à la fin de la chaîne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-118">The match must occur at the end of the string only.</span></span> <span data-ttu-id="e1a66-119">Pour plus d'informations, consultez [Fin de chaîne uniquement](#EndOnly).</span><span class="sxs-lookup"><span data-stu-id="e1a66-119">For more information, see [End of String Only](#EndOnly).</span></span>|  
|`\G`|<span data-ttu-id="e1a66-120">La correspondance doit démarrer à la position où la correspondance précédente s'est terminée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-120">The match must start at the position where the previous match ended.</span></span> <span data-ttu-id="e1a66-121">Pour plus d'informations, consultez [Correspondances contiguës](#Contiguous).</span><span class="sxs-lookup"><span data-stu-id="e1a66-121">For more information, see [Contiguous Matches](#Contiguous).</span></span>|  
|`\b`|<span data-ttu-id="e1a66-122">La correspondance doit se produire à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-122">The match must occur on a word boundary.</span></span> <span data-ttu-id="e1a66-123">Pour plus d'informations, consultez [Limite de mot](#WordBoundary).</span><span class="sxs-lookup"><span data-stu-id="e1a66-123">For more information, see [Word Boundary](#WordBoundary).</span></span>|  
|`\B`|<span data-ttu-id="e1a66-124">La correspondance ne doit pas se produire à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-124">The match must not occur on a word boundary.</span></span> <span data-ttu-id="e1a66-125">Pour plus d'informations, consultez [Limite n'appartenant pas à un mot](#NonwordBoundary).</span><span class="sxs-lookup"><span data-stu-id="e1a66-125">For more information, see [Non-Word Boundary](#NonwordBoundary).</span></span>|  
  
<a name="Start"></a>   
## <a name="start-of-string-or-line-"></a><span data-ttu-id="e1a66-126">Début de chaîne ou de ligne : ^</span><span class="sxs-lookup"><span data-stu-id="e1a66-126">Start of String or Line: ^</span></span>  
 <span data-ttu-id="e1a66-127">L'ancre `^` spécifie que le modèle suivant doit commencer à la première position de caractère de la chaîne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-127">The `^` anchor specifies that the following pattern must begin at the first character position of the string.</span></span> <span data-ttu-id="e1a66-128">Si vous utilisez `^` avec la <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option (consultez [Options des expressions régulières](../../../docs/standard/base-types/regular-expression-options.md)), la correspondance doit se produire au début de chaque ligne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-128">If you use `^` with the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option (see [Regular Expression Options](../../../docs/standard/base-types/regular-expression-options.md)), the match must occur at the beginning of each line.</span></span>  
  
 <span data-ttu-id="e1a66-129">L'exemple suivant utilise l'ancre `^` dans une expression régulière qui extrait des informations à propos des années pendant lesquelles certaines équipes de base-ball professionnelles ont existé.</span><span class="sxs-lookup"><span data-stu-id="e1a66-129">The following example uses the `^` anchor in a regular expression that extracts information about the years during which some professional baseball teams existed.</span></span> <span data-ttu-id="e1a66-130">L'exemple appelle deux surcharges de la méthode <xref:System.Text.RegularExpressions.Regex.Matches%2A?displayProperty=nameWithType> :</span><span class="sxs-lookup"><span data-stu-id="e1a66-130">The example calls two overloads of the <xref:System.Text.RegularExpressions.Regex.Matches%2A?displayProperty=nameWithType> method:</span></span>  
  
-   <span data-ttu-id="e1a66-131">L'appel à la surcharge <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%29> recherche uniquement la première sous-chaîne dans la chaîne d'entrée qui correspond au modèle d'expression régulière.</span><span class="sxs-lookup"><span data-stu-id="e1a66-131">The call to the <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%29> overload finds only the first substring in the input string that matches the regular expression pattern.</span></span>  
  
-   <span data-ttu-id="e1a66-132">L'appel à la surcharge <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29> avec le paramètre `options` défini avec la valeur <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> trouve les cinq sous-chaînes.</span><span class="sxs-lookup"><span data-stu-id="e1a66-132">The call to the <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29> overload with the `options` parameter set to <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> finds all five substrings.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#1](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/startofstring1.cs#1)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/startofstring1.vb#1)]  
  
 <span data-ttu-id="e1a66-133">Le modèle d'expression régulière `^((\w+(\s?)){2,}),\s(\w+\s\w+),(\s\d{4}(-(\d{4}|present))?,?)+` est défini comme indiqué dans le tableau suivant.</span><span class="sxs-lookup"><span data-stu-id="e1a66-133">The regular expression pattern `^((\w+(\s?)){2,}),\s(\w+\s\w+),(\s\d{4}(-(\d{4}|present))?,?)+` is defined as shown in the following table.</span></span>  
  
|<span data-ttu-id="e1a66-134">Modèle</span><span class="sxs-lookup"><span data-stu-id="e1a66-134">Pattern</span></span>|<span data-ttu-id="e1a66-135">Description</span><span class="sxs-lookup"><span data-stu-id="e1a66-135">Description</span></span>|  
|-------------|-----------------|  
|`^`|<span data-ttu-id="e1a66-136">Commencer la correspondance au début de la chaîne d'entrée (ou au début de la ligne si la méthode est appelée avec l'option <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>).</span><span class="sxs-lookup"><span data-stu-id="e1a66-136">Begin the match at the beginning of the input string (or the beginning of the line if the method is called with the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option).</span></span>|  
|`((\w+(\s?)){2,}`|<span data-ttu-id="e1a66-137">Mettre en correspondance un ou plusieurs caractères de mot suivis de zéro ou d'un espace précisément deux fois.</span><span class="sxs-lookup"><span data-stu-id="e1a66-137">Match one or more word characters followed either by zero or by one space exactly two times.</span></span> <span data-ttu-id="e1a66-138">Il s'agit du premier groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="e1a66-138">This is the first capturing group.</span></span> <span data-ttu-id="e1a66-139">Cette expression définit également un deuxième et un troisième groupe de capture : le deuxième se compose du mot capturé, et le troisième se compose des espaces capturés.</span><span class="sxs-lookup"><span data-stu-id="e1a66-139">This expression also defines a second and third capturing group: The second consists of the captured word, and the third consists of the captured spaces.</span></span>|  
|`,\s`|<span data-ttu-id="e1a66-140">Mettre en correspondance une virgule suivie d'un caractère d'espace blanc.</span><span class="sxs-lookup"><span data-stu-id="e1a66-140">Match a comma followed by a white-space character.</span></span>|  
|`(\w+\s\w+)`|<span data-ttu-id="e1a66-141">Mettre en correspondance un ou plusieurs caractères de mot suivis d'un espace, suivi d'un ou plusieurs caractères de mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-141">Match one or more word characters followed by a space, followed by one or more word characters.</span></span> <span data-ttu-id="e1a66-142">Il s'agit du quatrième groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="e1a66-142">This is the fourth capturing group.</span></span>|  
|`,`|<span data-ttu-id="e1a66-143">Mettre en correspondance une virgule.</span><span class="sxs-lookup"><span data-stu-id="e1a66-143">Match a comma.</span></span>|  
|`\s\d{4}`|<span data-ttu-id="e1a66-144">Mettre en correspondance un espace suivi de quatre chiffres décimaux.</span><span class="sxs-lookup"><span data-stu-id="e1a66-144">Match a space followed by four decimal digits.</span></span>|  
|<span data-ttu-id="e1a66-145">(-`(\d{4}&#124;present))?`</span><span class="sxs-lookup"><span data-stu-id="e1a66-145">(-`(\d{4}&#124;present))?`</span></span>|<span data-ttu-id="e1a66-146">Mettre en correspondance zéro ou une occurrence d'un trait d'union suivie de quatre chiffres décimaux ou de la chaîne « present ».</span><span class="sxs-lookup"><span data-stu-id="e1a66-146">Match zero or one occurrence of a hyphen followed by four decimal digits or the string "present".</span></span> <span data-ttu-id="e1a66-147">Il s'agit du sixième groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="e1a66-147">This is the sixth capturing group.</span></span> <span data-ttu-id="e1a66-148">Il inclut également un septième groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="e1a66-148">It also includes a seventh capturing group.</span></span>|  
|`,?`|<span data-ttu-id="e1a66-149">Mettre en correspondance zéro ou une occurrence d'une virgule.</span><span class="sxs-lookup"><span data-stu-id="e1a66-149">Match zero or one occurrence of a comma.</span></span>|  
|`(\s\d{4}(-(\d{4}&#124;present))?,?)+`|<span data-ttu-id="e1a66-150">Mettre en correspondance une ou plusieurs occurrences des éléments suivants : un espace, quatre chiffres décimaux, zéro ou une occurrence d'un trait d'union suivie de quatre chiffres décimaux ou de la chaîne « present » et zéro ou une virgule.</span><span class="sxs-lookup"><span data-stu-id="e1a66-150">Match one or more occurrences of the following: a space, four decimal digits, zero or one occurrence of a hyphen followed by four decimal digits or the string "present", and zero or one comma.</span></span> <span data-ttu-id="e1a66-151">Il s'agit du cinquième groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="e1a66-151">This is the fifth capturing group.</span></span>|  
  
 [<span data-ttu-id="e1a66-152">Retour au début</span><span class="sxs-lookup"><span data-stu-id="e1a66-152">Back to top</span></span>](#top)  
  
<a name="End"></a>   
## <a name="end-of-string-or-line-"></a><span data-ttu-id="e1a66-153">Fin de chaîne ou de ligne : $</span><span class="sxs-lookup"><span data-stu-id="e1a66-153">End of String or Line: $</span></span>  
 <span data-ttu-id="e1a66-154">L'ancre `$` spécifie que le modèle précédent doit se produire à la fin de la chaîne d'entrée ou avant `\n` à la fin de la chaîne d'entrée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-154">The `$` anchor specifies that the preceding pattern must occur at the end of the input string, or before `\n` at the end of the input string.</span></span>  
  
 <span data-ttu-id="e1a66-155">Si vous utilisez `$` avec l'option <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>, la correspondance peut également se trouver à la fin d'une ligne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-155">If you use `$` with the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option, the match can also occur at the end of a line.</span></span> <span data-ttu-id="e1a66-156">Notez que `$` correspond à `\n` , mais ne correspond pas à `\r\n` (combinaison de caractères de retour chariot et de saut de ligne ou CR/LF).</span><span class="sxs-lookup"><span data-stu-id="e1a66-156">Note that `$` matches `\n` but does not match `\r\n` (the combination of carriage return and newline characters, or CR/LF).</span></span> <span data-ttu-id="e1a66-157">Pour établir une correspondance avec la combinaison de caractères CR/LF, incluez `\r?$` dans le modèle d'expression régulière.</span><span class="sxs-lookup"><span data-stu-id="e1a66-157">To match the CR/LF character combination, include `\r?$` in the regular expression pattern.</span></span>  
  
 <span data-ttu-id="e1a66-158">L'exemple suivant ajoute l'ancre `$` au modèle d'expression régulière utilisé dans l'exemple dans la section [Début de chaîne ou de ligne](#Start) .</span><span class="sxs-lookup"><span data-stu-id="e1a66-158">The following example adds the `$` anchor to the regular expression pattern used in the example in the [Start of String or Line](#Start) section.</span></span> <span data-ttu-id="e1a66-159">En cas d'utilisation avec la chaîne d'entrée d'origine, qui inclut cinq lignes de texte, la méthode <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%29?displayProperty=nameWithType> ne peut pas trouver de correspondance, parce que la fin de la première ligne ne correspond pas au modèle `$`.</span><span class="sxs-lookup"><span data-stu-id="e1a66-159">When used with the original input string, which includes five lines of text, the <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%29?displayProperty=nameWithType> method is unable to find a match, because the end of the first line does not match the `$` pattern.</span></span> <span data-ttu-id="e1a66-160">Quand la chaîne d'entrée d'origine est fractionnée dans un tableau de chaînes, la méthode <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%29?displayProperty=nameWithType> réussit à faire correspondre chacune des cinq lignes.</span><span class="sxs-lookup"><span data-stu-id="e1a66-160">When the original input string is split into a string array, the <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%29?displayProperty=nameWithType> method succeeds in matching each of the five lines.</span></span> <span data-ttu-id="e1a66-161">Quand la méthode <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> est appelée avec le paramètre `options` défini avec la valeur <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>, aucune correspondance n'est trouvée parce que le modèle d'expression régulière ne représente pas l'élément de retour chariot (\u+000D).</span><span class="sxs-lookup"><span data-stu-id="e1a66-161">When the <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> method is called with the `options` parameter set to <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>, no matches are found because the regular expression pattern does not account for the carriage return element (\u+000D).</span></span> <span data-ttu-id="e1a66-162">Toutefois, quand le modèle d'expression régulière est modifié par le remplacement de `$` par `\r?$`, l'appel de la méthode <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> avec le paramètre `options` défini avec la valeur <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> trouve encore cinq correspondances.</span><span class="sxs-lookup"><span data-stu-id="e1a66-162">However, when the regular expression pattern is modified by replacing `$` with `\r?$`, calling the <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> method with the `options` parameter set to <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> again finds five matches.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#2](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/endofstring1.cs#2)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/endofstring1.vb#2)]  
  
 [<span data-ttu-id="e1a66-163">Retour au début</span><span class="sxs-lookup"><span data-stu-id="e1a66-163">Back to top</span></span>](#top)  
  
<a name="StartOnly"></a>   
## <a name="start-of-string-only-a"></a><span data-ttu-id="e1a66-164">Début de chaîne uniquement : \A</span><span class="sxs-lookup"><span data-stu-id="e1a66-164">Start of String Only: \A</span></span>  
 <span data-ttu-id="e1a66-165">L'ancre `\A` spécifie qu'une correspondance doit se produire au début de la chaîne d'entrée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-165">The `\A` anchor specifies that a match must occur at the beginning of the input string.</span></span> <span data-ttu-id="e1a66-166">Elle est identique à l'ancre `^`, à la différence près que l'ancre `\A` ignore l'option <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e1a66-166">It is identical to the `^` anchor, except that `\A` ignores the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option.</span></span> <span data-ttu-id="e1a66-167">Par conséquent, elle peut correspondre uniquement au début de la première ligne dans une chaîne d'entrée multiligne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-167">Therefore, it can only match the start of the first line in a multiline input string.</span></span>  
  
 <span data-ttu-id="e1a66-168">L'exemple suivant est semblable aux exemples des ancres `^` et `$` .</span><span class="sxs-lookup"><span data-stu-id="e1a66-168">The following example is similar to the examples for the `^` and `$` anchors.</span></span> <span data-ttu-id="e1a66-169">Il utilise l'ancre `\A` dans une expression régulière qui extrait des informations sur les années où jouaient certaines équipes de base-ball professionnelles.</span><span class="sxs-lookup"><span data-stu-id="e1a66-169">It uses the `\A` anchor in a regular expression that extracts information about the years during which some professional baseball teams existed.</span></span> <span data-ttu-id="e1a66-170">La chaîne d'entrée inclut cinq lignes.</span><span class="sxs-lookup"><span data-stu-id="e1a66-170">The input string includes five lines.</span></span> <span data-ttu-id="e1a66-171">L'appel à la méthode <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> recherche uniquement la première sous-chaîne dans la chaîne d'entrée qui correspond au modèle d'expression régulière.</span><span class="sxs-lookup"><span data-stu-id="e1a66-171">The call to the <xref:System.Text.RegularExpressions.Regex.Matches%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> method finds only the first substring in the input string that matches the regular expression pattern.</span></span> <span data-ttu-id="e1a66-172">Comme le montre l'exemple, l'option <xref:System.Text.RegularExpressions.RegexOptions.Multiline> n'a aucun effet.</span><span class="sxs-lookup"><span data-stu-id="e1a66-172">As the example shows, the <xref:System.Text.RegularExpressions.RegexOptions.Multiline> option has no effect.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#3](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/startofstring2.cs#3)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/startofstring2.vb#3)]  
  
 [<span data-ttu-id="e1a66-173">Retour au début</span><span class="sxs-lookup"><span data-stu-id="e1a66-173">Back to top</span></span>](#top)  
  
<a name="EndOrNOnly"></a>   
## <a name="end-of-string-or-before-ending-newline-z"></a><span data-ttu-id="e1a66-174">Fin de chaîne ou avant un saut de ligne final : \Z</span><span class="sxs-lookup"><span data-stu-id="e1a66-174">End of String or Before Ending Newline: \Z</span></span>  
 <span data-ttu-id="e1a66-175">L'ancre `\Z` spécifie qu'une correspondance doit se produire à la fin de la chaîne d'entrée ou avant `\n` à la fin de la chaîne d'entrée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-175">The `\Z` anchor specifies that a match must occur at the end of the input string, or before `\n` at the end of the input string.</span></span> <span data-ttu-id="e1a66-176">Elle est identique à l'ancre `$`, à la différence près que l'ancre `\Z` ignore l'option <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e1a66-176">It is identical to the `$` anchor, except that `\Z` ignores the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option.</span></span> <span data-ttu-id="e1a66-177">Par conséquent, dans une chaîne multiligne, elle peut correspondre uniquement à la fin de la dernière ligne ou à la dernière ligne avant `\n`.</span><span class="sxs-lookup"><span data-stu-id="e1a66-177">Therefore, in a multiline string, it can only match the end of the last line, or the last line before `\n`.</span></span>  
  
 <span data-ttu-id="e1a66-178">Notez que `\Z` correspond à `\n` , mais ne correspond pas à `\r\n` (combinaison de caractères CR/LF).</span><span class="sxs-lookup"><span data-stu-id="e1a66-178">Note that `\Z` matches `\n` but does not match `\r\n` (the CR/LF character combination).</span></span> <span data-ttu-id="e1a66-179">Pour établir une correspondance avec les caractères CR/LF, incluez `\r?\Z` dans le modèle d'expression régulière.</span><span class="sxs-lookup"><span data-stu-id="e1a66-179">To match CR/LF, include `\r?\Z` in the regular expression pattern.</span></span>  
  
 <span data-ttu-id="e1a66-180">L'exemple suivant utilise l'ancre `\Z` dans une expression régulière qui est semblable à l'exemple dans la section [Début de chaîne ou de ligne](#Start) , qui extrait des informations à propos des années pendant lesquelles certaines équipes de base-ball professionnelles ont existé.</span><span class="sxs-lookup"><span data-stu-id="e1a66-180">The following example uses the `\Z` anchor in a regular expression that is similar to the example in the [Start of String or Line](#Start) section, which extracts information about the years during which some professional baseball teams existed.</span></span> <span data-ttu-id="e1a66-181">La sous-expression `\r?\Z` dans l'expression régulière `^((\w+(\s?)){2,}),\s(\w+\s\w+),(\s\d{4}(-(\d{4}|present))?,?)+\r?\Z` correspond à la fin d'une chaîne et correspond également à une chaîne qui se termine par `\n` ou `\r\n`.</span><span class="sxs-lookup"><span data-stu-id="e1a66-181">The subexpression `\r?\Z` in the regular expression `^((\w+(\s?)){2,}),\s(\w+\s\w+),(\s\d{4}(-(\d{4}|present))?,?)+\r?\Z` matches the end of a string, and also matches a string that ends with `\n` or `\r\n`.</span></span> <span data-ttu-id="e1a66-182">Par conséquent, chaque élément du tableau correspond au modèle d'expression régulière.</span><span class="sxs-lookup"><span data-stu-id="e1a66-182">As a result, each element in the array matches the regular expression pattern.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#4](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/endofstring2.cs#4)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/endofstring2.vb#4)]  
  
 [<span data-ttu-id="e1a66-183">Retour au début</span><span class="sxs-lookup"><span data-stu-id="e1a66-183">Back to top</span></span>](#top)  
  
<a name="EndOnly"></a>   
## <a name="end-of-string-only-z"></a><span data-ttu-id="e1a66-184">Fin de chaîne uniquement : \z</span><span class="sxs-lookup"><span data-stu-id="e1a66-184">End of String Only: \z</span></span>  
 <span data-ttu-id="e1a66-185">L'ancre `\z` spécifie qu'une correspondance doit se produire à la fin de la chaîne d'entrée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-185">The `\z` anchor specifies that a match must occur at the end of the input string.</span></span> <span data-ttu-id="e1a66-186">Comme l'élément de langage `$`, `\z` ignore l'option <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e1a66-186">Like the `$` language element, `\z` ignores the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option.</span></span> <span data-ttu-id="e1a66-187">Contrairement à l'élément de langage `\Z` , `\z` ne correspond pas à un caractère `\n` à la fin d'une chaîne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-187">Unlike the `\Z` language element, `\z` does not match a `\n` character at the end of a string.</span></span> <span data-ttu-id="e1a66-188">Par conséquent, elle peut correspondre uniquement à la dernière ligne de la chaîne d'entrée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-188">Therefore, it can only match the last line of the input string.</span></span>  
  
 <span data-ttu-id="e1a66-189">L'exemple suivant utilise l'ancre `\z` dans une expression régulière qui est sinon identique à l'exemple dans la section précédente, qui extrait des informations à propos des années pendant lesquelles certaines équipes de base-ball professionnelles ont existé.</span><span class="sxs-lookup"><span data-stu-id="e1a66-189">The following example uses the `\z` anchor in a regular expression that is otherwise identical to the example in the previous section, which extracts information about the years during which some professional baseball teams existed.</span></span> <span data-ttu-id="e1a66-190">L'exemple essaie de faire correspondre chacun des cinq éléments dans un tableau de chaînes avec le modèle d'expression régulière `^((\w+(\s?)){2,}),\s(\w+\s\w+),(\s\d{4}(-(\d{4}|present))?,?)+\r?\z`.</span><span class="sxs-lookup"><span data-stu-id="e1a66-190">The example tries to match each of five elements in a string array with the regular expression pattern `^((\w+(\s?)){2,}),\s(\w+\s\w+),(\s\d{4}(-(\d{4}|present))?,?)+\r?\z`.</span></span> <span data-ttu-id="e1a66-191">Deux des chaînes se terminent par un retour chariot et des caractères de saut de ligne, l'une se termine par un caractère de saut de ligne, et deux ne se terminent ni par un retour chariot ni par un caractère de saut de ligne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-191">Two of the strings end with carriage return and line feed characters, one ends with a line feed character, and two end with neither a carriage return nor a line feed character.</span></span> <span data-ttu-id="e1a66-192">Comme le montre la sortie, seules les chaînes sans retour chariot ou caractère de saut de ligne correspondent au modèle.</span><span class="sxs-lookup"><span data-stu-id="e1a66-192">As the output shows, only the strings without a carriage return or line feed character match the pattern.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#5](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/endofstring3.cs#5)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/endofstring3.vb#5)]  
  
 [<span data-ttu-id="e1a66-193">Retour au début</span><span class="sxs-lookup"><span data-stu-id="e1a66-193">Back to top</span></span>](#top)  
  
<a name="Contiguous"></a>   
## <a name="contiguous-matches-g"></a><span data-ttu-id="e1a66-194">Correspondances contiguës : \G</span><span class="sxs-lookup"><span data-stu-id="e1a66-194">Contiguous Matches: \G</span></span>  
 <span data-ttu-id="e1a66-195">L'ancre `\G` spécifie qu'une correspondance doit se produire au point où la correspondance précédente s'est terminée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-195">The `\G` anchor specifies that a match must occur at the point where the previous match ended.</span></span> <span data-ttu-id="e1a66-196">Quand vous utilisez cette ancre avec la méthode <xref:System.Text.RegularExpressions.Regex.Matches%2A?displayProperty=nameWithType> ou <xref:System.Text.RegularExpressions.Match.NextMatch%2A?displayProperty=nameWithType>, elle vérifie que toutes les correspondances sont contiguës.</span><span class="sxs-lookup"><span data-stu-id="e1a66-196">When you use this anchor with the <xref:System.Text.RegularExpressions.Regex.Matches%2A?displayProperty=nameWithType> or <xref:System.Text.RegularExpressions.Match.NextMatch%2A?displayProperty=nameWithType> method, it ensures that all matches are contiguous.</span></span>  
  
 <span data-ttu-id="e1a66-197">L'exemple suivant utilise une expression régulière pour extraire les noms d'espèces de rongeurs d'une chaîne délimitée par des virgules.</span><span class="sxs-lookup"><span data-stu-id="e1a66-197">The following example uses a regular expression to extract the names of rodent species from a comma-delimited string.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#6](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/contiguous1.cs#6)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#6](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/contiguous1.vb#6)]  
  
 <span data-ttu-id="e1a66-198">L'expression régulière `\G(\w+\s?\w*),?` est interprétée comme indiqué dans le tableau suivant.</span><span class="sxs-lookup"><span data-stu-id="e1a66-198">The regular expression `\G(\w+\s?\w*),?` is interpreted as shown in the following table.</span></span>  
  
|<span data-ttu-id="e1a66-199">Modèle</span><span class="sxs-lookup"><span data-stu-id="e1a66-199">Pattern</span></span>|<span data-ttu-id="e1a66-200">Description</span><span class="sxs-lookup"><span data-stu-id="e1a66-200">Description</span></span>|  
|-------------|-----------------|  
|`\G`|<span data-ttu-id="e1a66-201">Commencer là où la dernière correspondance s'est terminée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-201">Begin where the last match ended.</span></span>|  
|`\w+`|<span data-ttu-id="e1a66-202">Mettre en correspondance un ou plusieurs caractères alphabétiques.</span><span class="sxs-lookup"><span data-stu-id="e1a66-202">Match one or more word characters.</span></span>|  
|`\s?`|<span data-ttu-id="e1a66-203">Mettre en correspondance zéro ou un espace.</span><span class="sxs-lookup"><span data-stu-id="e1a66-203">Match zero or one space.</span></span>|  
|`\w*`|<span data-ttu-id="e1a66-204">Mettre en correspondance zéro, un ou plusieurs caractères alphabétiques.</span><span class="sxs-lookup"><span data-stu-id="e1a66-204">Match zero or more word characters.</span></span>|  
|`(\w+\s?\w*)`|<span data-ttu-id="e1a66-205">Mettre en correspondance un ou plusieurs caractères de mot suivis de zéro ou d'un espace, suivi de zéro ou davantage de caractères de mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-205">Match one or more word characters followed by zero or one space, followed by zero or more word characters.</span></span> <span data-ttu-id="e1a66-206">Il s'agit du premier groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="e1a66-206">This is the first capturing group.</span></span>|  
|`,?`|<span data-ttu-id="e1a66-207">Mettre en correspondance zéro ou une occurrence d'une virgule littérale.</span><span class="sxs-lookup"><span data-stu-id="e1a66-207">Match zero or one occurrence of a literal comma character.</span></span>|  
  
 [<span data-ttu-id="e1a66-208">Retour au début</span><span class="sxs-lookup"><span data-stu-id="e1a66-208">Back to top</span></span>](#top)  
  
<a name="WordBoundary"></a>   
## <a name="word-boundary-b"></a><span data-ttu-id="e1a66-209">Limite de mot : \b</span><span class="sxs-lookup"><span data-stu-id="e1a66-209">Word Boundary: \b</span></span>  
 <span data-ttu-id="e1a66-210">L'ancre `\b` spécifie que la correspondance doit se produire à la limite entre un caractère de mot (élément de langage `\w` ) et un caractère n'appartenant pas à un mot (élément de langage `\W` ).</span><span class="sxs-lookup"><span data-stu-id="e1a66-210">The `\b` anchor specifies that the match must occur on a boundary between a word character (the `\w` language element) and a non-word character (the `\W` language element).</span></span> <span data-ttu-id="e1a66-211">Les caractères de mot se composent de caractères alphanumériques et de traits de soulignement ; un caractère n'appartenant pas à un mot est un caractère qui n'est pas alphanumérique ou qui n'est pas un trait de soulignement.</span><span class="sxs-lookup"><span data-stu-id="e1a66-211">Word characters consist of alphanumeric characters and underscores; a non-word character is any character that is not alphanumeric or an underscore.</span></span> <span data-ttu-id="e1a66-212">(Pour plus d’informations, consultez [Classes de caractères](../../../docs/standard/base-types/character-classes-in-regular-expressions.md).) La correspondance peut également se produire à la limite d'un mot au début ou à la fin de la chaîne.</span><span class="sxs-lookup"><span data-stu-id="e1a66-212">(For more information, see [Character Classes](../../../docs/standard/base-types/character-classes-in-regular-expressions.md).) The match may also occur on a word boundary at the beginning or end of the string.</span></span>  
  
 <span data-ttu-id="e1a66-213">L'ancre `\b` est fréquemment utilisée pour faire en sorte qu'une sous-expression corresponde à un mot entier plutôt qu'au début ou à la fin d'un mot uniquement.</span><span class="sxs-lookup"><span data-stu-id="e1a66-213">The `\b` anchor is frequently used to ensure that a subexpression matches an entire word instead of just the beginning or end of a word.</span></span> <span data-ttu-id="e1a66-214">L'expression régulière `\bare\w*\b` dans l'exemple suivant illustre cette utilisation.</span><span class="sxs-lookup"><span data-stu-id="e1a66-214">The regular expression `\bare\w*\b` in the following example illustrates this usage.</span></span> <span data-ttu-id="e1a66-215">Elle correspond à tout mot qui commence par la sous-chaîne « are ».</span><span class="sxs-lookup"><span data-stu-id="e1a66-215">It matches any word that begins with the substring "are".</span></span> <span data-ttu-id="e1a66-216">La sortie de l'exemple illustre également que `\b` correspond à la fois au début et la fin de la chaîne d'entrée.</span><span class="sxs-lookup"><span data-stu-id="e1a66-216">The output from the example also illustrates that `\b` matches both the beginning and the end of the input string.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#7](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/word1.cs#7)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#7](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/word1.vb#7)]  
  
 <span data-ttu-id="e1a66-217">Le modèle d'expression régulière est interprété comme indiqué dans le tableau suivant.</span><span class="sxs-lookup"><span data-stu-id="e1a66-217">The regular expression pattern is interpreted as shown in the following table.</span></span>  
  
|<span data-ttu-id="e1a66-218">Modèle</span><span class="sxs-lookup"><span data-stu-id="e1a66-218">Pattern</span></span>|<span data-ttu-id="e1a66-219">Description</span><span class="sxs-lookup"><span data-stu-id="e1a66-219">Description</span></span>|  
|-------------|-----------------|  
|`\b`|<span data-ttu-id="e1a66-220">Commencer la correspondance à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-220">Begin the match at a word boundary.</span></span>|  
|`are`|<span data-ttu-id="e1a66-221">Mettre en correspondance la sous-chaîne « are ».</span><span class="sxs-lookup"><span data-stu-id="e1a66-221">Match the substring "are".</span></span>|  
|`\w*`|<span data-ttu-id="e1a66-222">Mettre en correspondance zéro, un ou plusieurs caractères alphabétiques.</span><span class="sxs-lookup"><span data-stu-id="e1a66-222">Match zero or more word characters.</span></span>|  
|`\b`|<span data-ttu-id="e1a66-223">Terminer la correspondance à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-223">End the match at a word boundary.</span></span>|  
  
 [<span data-ttu-id="e1a66-224">Retour au début</span><span class="sxs-lookup"><span data-stu-id="e1a66-224">Back to top</span></span>](#top)  
  
<a name="NonwordBoundary"></a>   
## <a name="non-word-boundary-b"></a><span data-ttu-id="e1a66-225">Limite n'appartenant pas à un mot : \B</span><span class="sxs-lookup"><span data-stu-id="e1a66-225">Non-Word Boundary: \B</span></span>  
 <span data-ttu-id="e1a66-226">L'ancre `\B` spécifie que la correspondance ne doit pas se produire à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-226">The `\B` anchor specifies that the match must not occur on a word boundary.</span></span> <span data-ttu-id="e1a66-227">Elle est le contraire de l'ancre `\b` .</span><span class="sxs-lookup"><span data-stu-id="e1a66-227">It is the opposite of the `\b` anchor.</span></span>  
  
 <span data-ttu-id="e1a66-228">L'exemple suivant utilise l'ancre `\B` pour trouver des occurrences de la sous-chaîne « qu » dans un mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-228">The following example uses the `\B` anchor to locate occurrences of the substring "qu" in a word.</span></span> <span data-ttu-id="e1a66-229">Le modèle d'expression régulière `\Bqu\w+` met en correspondance une sous-chaîne qui commence par un « qu » qui n'est pas en début de mot et continue jusqu'à la fin du mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-229">The regular expression pattern `\Bqu\w+` matches a substring that begins with a "qu" that does not start a word and that continues to the end of the word.</span></span>  
  
 [!code-csharp[Conceptual.RegEx.Language.Assertions#8](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.assertions/cs/nonword1.cs#8)]
 [!code-vb[Conceptual.RegEx.Language.Assertions#8](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.assertions/vb/nonword1.vb#8)]  
  
 <span data-ttu-id="e1a66-230">Le modèle d'expression régulière est interprété comme indiqué dans le tableau suivant.</span><span class="sxs-lookup"><span data-stu-id="e1a66-230">The regular expression pattern is interpreted as shown in the following table.</span></span>  
  
|<span data-ttu-id="e1a66-231">Modèle</span><span class="sxs-lookup"><span data-stu-id="e1a66-231">Pattern</span></span>|<span data-ttu-id="e1a66-232">Description</span><span class="sxs-lookup"><span data-stu-id="e1a66-232">Description</span></span>|  
|-------------|-----------------|  
|`\B`|<span data-ttu-id="e1a66-233">Ne pas commencer la correspondance à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="e1a66-233">Do not begin the match at a word boundary.</span></span>|  
|`qu`|<span data-ttu-id="e1a66-234">Mettre en correspondance la sous-chaîne « qu ».</span><span class="sxs-lookup"><span data-stu-id="e1a66-234">Match the substring "qu".</span></span>|  
|`\w+`|<span data-ttu-id="e1a66-235">Mettre en correspondance un ou plusieurs caractères alphabétiques.</span><span class="sxs-lookup"><span data-stu-id="e1a66-235">Match one or more word characters.</span></span>|  
  
## <a name="see-also"></a><span data-ttu-id="e1a66-236">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="e1a66-236">See Also</span></span>  
 [<span data-ttu-id="e1a66-237">Langage des expressions régulières - Aide-mémoire</span><span class="sxs-lookup"><span data-stu-id="e1a66-237">Regular Expression Language - Quick Reference</span></span>](../../../docs/standard/base-types/regular-expression-language-quick-reference.md)  
 [<span data-ttu-id="e1a66-238">Options des expressions régulières</span><span class="sxs-lookup"><span data-stu-id="e1a66-238">Regular Expression Options</span></span>](../../../docs/standard/base-types/regular-expression-options.md)
