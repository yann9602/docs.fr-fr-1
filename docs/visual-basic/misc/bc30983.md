---
title: "Type de &#39; &lt;variablename&gt;&#39; est ambiguë, car les limites de la boucle et du s’étend pas vers le même type"
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-visual-basic
ms.topic: article
f1_keywords:
- vbc30983
- bc30983
helpviewer_keywords: BC30983
ms.assetid: 6b97153c-dee3-4429-b92a-2e5a212c864b
caps.latest.revision: "14"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 38b3910711b689d16d97cf9f506a927b660a0cbe
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/21/2017
---
# <a name="type-of-39ltvariablenamegt39-is-ambiguous-because-the-loop-bounds-and-the-step-variable-do-not-widen-to-the-same-type"></a><span data-ttu-id="ac446-102">Type de &#39; &lt;variablename&gt;&#39; est ambiguë, car les limites de la boucle et du s’étend pas vers le même type</span><span class="sxs-lookup"><span data-stu-id="ac446-102">Type of &#39;&lt;variablename&gt;&#39; is ambiguous because the loop bounds and the step variable do not widen to the same type</span></span>
<span data-ttu-id="ac446-103">Votre code contient une boucle `For...Next` dans laquelle le compilateur ne peut pas déduire un type de données pour la variable de contrôle de boucle, car les conditions suivantes sont remplies :</span><span class="sxs-lookup"><span data-stu-id="ac446-103">Your code contains a `For...Next` loop in which the compiler cannot infer a data type for the loop control variable because the following conditions is true:</span></span>  
  
-   <span data-ttu-id="ac446-104">Le type de données de la variable de contrôle de boucle n’est pas spécifié avec une clause `As` .</span><span class="sxs-lookup"><span data-stu-id="ac446-104">The data type of the loop control variable is not specified with an `As` clause.</span></span>  
  
-   <span data-ttu-id="ac446-105">Les limites de la boucle et l’incrémentation contiennent au moins deux types de données.</span><span class="sxs-lookup"><span data-stu-id="ac446-105">The loop bounds and step variable contain at least two data types.</span></span>  
  
-   <span data-ttu-id="ac446-106">Il existe plusieurs conversions possibles entre les types de données.</span><span class="sxs-lookup"><span data-stu-id="ac446-106">More than one possible conversion exists between the data types.</span></span>  
  
-   <span data-ttu-id="ac446-107">Il n’existe aucun meilleur type parmi les candidats, si bien que le choix du type pour la variable de contrôle de boucle est ambigu.</span><span class="sxs-lookup"><span data-stu-id="ac446-107">There is no best type among the candidates, so that the choice of type for the loop control variable is ambiguous.</span></span>  
  
 <span data-ttu-id="ac446-108">Par exemple, la boucle suivante a une limite de boucle de type `Integer` et une autre de type `UInteger`:</span><span class="sxs-lookup"><span data-stu-id="ac446-108">For example, the following loop has one loop bound of type `Integer` and one loop bound of type `UInteger`:</span></span>  
  
```vb  
Dim m As Integer = 1  
Dim n As UInteger = 10  
' Not valid.  
' For i = m To n  
    ' Loop processing.  
' Next  
```  
  
 <span data-ttu-id="ac446-109">Il existe une conversion possible entre `Integer` et `UInteger`, et une conversion possible entre `UInteger` et `Integer`, mais les deux sont des conversions restrictives, donc aucune ne constitue le meilleur choix.</span><span class="sxs-lookup"><span data-stu-id="ac446-109">There is a possible conversion between `Integer` and `UInteger`, and a possible conversion between `UInteger` and `Integer`, but both are narrowing conversions so neither is the best choice.</span></span>  
  
 <span data-ttu-id="ac446-110">Dans l’exemple suivant, une troisième variable de type `Double` est ajoutée.</span><span class="sxs-lookup"><span data-stu-id="ac446-110">In the next example, a third variable of type `Double` is added.</span></span> <span data-ttu-id="ac446-111">`Integer` et `UInteger` ont des conversions étendues standard en `Double`, ce qui fait de la conversion en `Double` le meilleur choix.</span><span class="sxs-lookup"><span data-stu-id="ac446-111">Both `Integer` and `UInteger` have standard widening conversions to `Double`, which makes conversion to `Double` the best choice.</span></span> <span data-ttu-id="ac446-112">L’inférence de type assigne à la variable de contrôle de boucle `i` le type de données `Double`.</span><span class="sxs-lookup"><span data-stu-id="ac446-112">Type inference assigns to loop control variable `i` the data type `Double`.</span></span>  
  
```vb  
Dim stepVar As Double = 1  
' Valid.  
For i = m To n Step stepVar  
    ' Loop processing.  
Next  
```  
  
 <span data-ttu-id="ac446-113">**ID d’erreur :** BC30983</span><span class="sxs-lookup"><span data-stu-id="ac446-113">**Error ID:** BC30983</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="ac446-114">Pour corriger cette erreur</span><span class="sxs-lookup"><span data-stu-id="ac446-114">To correct this error</span></span>  
  
-   <span data-ttu-id="ac446-115">Convertissez explicitement l’une des variables en un type vers lequel les autres ont une conversion étendue, par exemple :</span><span class="sxs-lookup"><span data-stu-id="ac446-115">Explicitly convert one of the variables to a type that the others have a widening conversion to, for example:</span></span>  
  
    ```  
    For i = m To CLng(n)  
    ```  
  
-   <span data-ttu-id="ac446-116">Utilisez une clause `As` pour spécifier le type de la variable de contrôle :</span><span class="sxs-lookup"><span data-stu-id="ac446-116">Use an `As` clause to specify the type of the control variable:</span></span>  
  
    ```  
    For i As Double = m To n   
    ```  
  
## <a name="see-also"></a><span data-ttu-id="ac446-117">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="ac446-117">See Also</span></span>  
 [<span data-ttu-id="ac446-118">For...Next (instruction)</span><span class="sxs-lookup"><span data-stu-id="ac446-118">For...Next Statement</span></span>](../../visual-basic/language-reference/statements/for-next-statement.md)  
 [<span data-ttu-id="ac446-119">Conversions implicites et explicites</span><span class="sxs-lookup"><span data-stu-id="ac446-119">Implicit and Explicit Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/implicit-and-explicit-conversions.md)  
 [<span data-ttu-id="ac446-120">Inférence de type local</span><span class="sxs-lookup"><span data-stu-id="ac446-120">Local Type Inference</span></span>](../../visual-basic/programming-guide/language-features/variables/local-type-inference.md)  
 [<span data-ttu-id="ac446-121">Option Infer (instruction)</span><span class="sxs-lookup"><span data-stu-id="ac446-121">Option Infer Statement</span></span>](../../visual-basic/language-reference/statements/option-infer-statement.md)  
 [<span data-ttu-id="ac446-122">Conversions étendues et restrictives</span><span class="sxs-lookup"><span data-stu-id="ac446-122">Widening and Narrowing Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md)
