---
title: "La gestion des événements (Visual Basic)"
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
caps.latest.revision: "18"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: e4e31937d67d2140865a9626f79fbddc16796709
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/21/2017
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="5375a-102">Procédure pas à pas : gestion des événements (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="5375a-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="5375a-103">Il s’agit de la deuxième des deux rubriques qui montrent comment utiliser des événements.</span><span class="sxs-lookup"><span data-stu-id="5375a-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="5375a-104">La première rubrique [procédure pas à pas : déclaration et déclenchement des événements](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), montre comment déclarer et déclencher des événements.</span><span class="sxs-lookup"><span data-stu-id="5375a-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="5375a-105">Cette section utilise le formulaire et la classe à partir de cette procédure pas à pas pour montrer comment gérer les événements lorsqu’ils se produisent.</span><span class="sxs-lookup"><span data-stu-id="5375a-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="5375a-106">Le `Widget` exemple de classe utilise des instructions de gestion des événements traditionnelles.</span><span class="sxs-lookup"><span data-stu-id="5375a-106">The `Widget` class example uses traditional event-handling statements.</span></span> [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)]<span data-ttu-id="5375a-107">fournit d’autres techniques pour l’utilisation des événements.</span><span class="sxs-lookup"><span data-stu-id="5375a-107"> provides other techniques for working with events.</span></span> <span data-ttu-id="5375a-108">En guise d’exercice, vous pouvez modifier cet exemple montre comment utiliser le `AddHandler` et `Handles` instructions.</span><span class="sxs-lookup"><span data-stu-id="5375a-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="5375a-109">Pour gérer l’événement PercentDone de la classe Widget</span><span class="sxs-lookup"><span data-stu-id="5375a-109">To handle the PercentDone event of the Widget class</span></span>  
  
1.  <span data-ttu-id="5375a-110">Placez le code suivant dans `Form1`:</span><span class="sxs-lookup"><span data-stu-id="5375a-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_1.vb)]  
  
     <span data-ttu-id="5375a-111">Le `WithEvents` (mot clé) Spécifie que la variable `mWidget` est utilisée pour gérer les événements d’un objet.</span><span class="sxs-lookup"><span data-stu-id="5375a-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="5375a-112">Vous spécifiez le type d’objet en fournissant le nom de la classe à partir de laquelle l’objet sera créé.</span><span class="sxs-lookup"><span data-stu-id="5375a-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="5375a-113">La variable `mWidget` est déclarée dans `Form1` car `WithEvents` variables doivent être de niveau classe.</span><span class="sxs-lookup"><span data-stu-id="5375a-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="5375a-114">Cela est vrai quel que soit le type de classe, dans que placez-les.</span><span class="sxs-lookup"><span data-stu-id="5375a-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="5375a-115">La variable `mblnCancel` est utilisée pour annuler le `LongTask` (méthode).</span><span class="sxs-lookup"><span data-stu-id="5375a-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="5375a-116">Écriture de Code pour gérer un événement</span><span class="sxs-lookup"><span data-stu-id="5375a-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="5375a-117">Dès que vous déclarez une variable à l’aide `WithEvents`, le nom de la variable s’affiche dans la liste déroulante gauche de la classe **éditeur de Code**.</span><span class="sxs-lookup"><span data-stu-id="5375a-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="5375a-118">Lorsque vous sélectionnez `mWidget`, le `Widget` événements de la classe apparaissent dans la liste déroulante droite.</span><span class="sxs-lookup"><span data-stu-id="5375a-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="5375a-119">Si un événement est sélectionnée, la procédure événementielle correspondante, avec le préfixe `mWidget` et un trait de soulignement.</span><span class="sxs-lookup"><span data-stu-id="5375a-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="5375a-120">Toutes les procédures d’événement associés à un `WithEvents` variable sont fournies en tant que préfixe du nom de la variable.</span><span class="sxs-lookup"><span data-stu-id="5375a-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="5375a-121">Pour gérer un événement</span><span class="sxs-lookup"><span data-stu-id="5375a-121">To handle an event</span></span>  
  
1.  <span data-ttu-id="5375a-122">Sélectionnez `mWidget` dans la liste déroulante dans le **éditeur de Code**.</span><span class="sxs-lookup"><span data-stu-id="5375a-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2.  <span data-ttu-id="5375a-123">Sélectionnez le `PercentDone` événement dans la liste déroulante droite.</span><span class="sxs-lookup"><span data-stu-id="5375a-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="5375a-124">Le **éditeur de Code** ouvre le `mWidget_PercentDone` procédure événementielle.</span><span class="sxs-lookup"><span data-stu-id="5375a-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="5375a-125">Le **éditeur de Code** est utile, mais pas obligatoire, pour l’insertion de nouveaux gestionnaires d’événements.</span><span class="sxs-lookup"><span data-stu-id="5375a-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="5375a-126">Dans cette procédure pas à pas, il est plus directe copier simplement les gestionnaires d’événements directement dans votre code.</span><span class="sxs-lookup"><span data-stu-id="5375a-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3.  <span data-ttu-id="5375a-127">Ajoutez le code suivant au gestionnaire d'événements `mWidget_PercentDone` :</span><span class="sxs-lookup"><span data-stu-id="5375a-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_2.vb)]  
  
     <span data-ttu-id="5375a-128">Chaque fois que le `PercentDone` événement est déclenché, la procédure événementielle affiche le pourcentage d’achèvement dans un `Label` contrôle.</span><span class="sxs-lookup"><span data-stu-id="5375a-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="5375a-129">Le `DoEvents` méthode permet de l’étiquette soit repeinte et donne également à l’utilisateur la possibilité de cliquer sur le **Annuler** bouton.</span><span class="sxs-lookup"><span data-stu-id="5375a-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4.  <span data-ttu-id="5375a-130">Ajoutez le code suivant pour le `Button2_Click` Gestionnaire d’événements :</span><span class="sxs-lookup"><span data-stu-id="5375a-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_3.vb)]  
  
 <span data-ttu-id="5375a-131">Si l’utilisateur clique sur le **Annuler** bouton lors de la `LongTask` est en cours d’exécution, le `Button2_Click` événement est exécuté dès que le `DoEvents` instruction permet le traitement de l’événement.</span><span class="sxs-lookup"><span data-stu-id="5375a-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="5375a-132">La variable de niveau classe `mblnCancel` a la valeur `True`et le `mWidget_PercentDone` événement, puis le teste et définit le `ByRef Cancel` argument `True`.</span><span class="sxs-lookup"><span data-stu-id="5375a-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="5375a-133">Connexion d’une Variable WithEvents à un objet</span><span class="sxs-lookup"><span data-stu-id="5375a-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="5375a-134">`Form1`est maintenant configuré pour gérer un `Widget` événements d’objet.</span><span class="sxs-lookup"><span data-stu-id="5375a-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="5375a-135">Il ne reste qu’à trouver un `Widget` quelque part.</span><span class="sxs-lookup"><span data-stu-id="5375a-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="5375a-136">Lorsque vous déclarez une variable `WithEvents` au moment du design, aucun objet ne lui n’est associé.</span><span class="sxs-lookup"><span data-stu-id="5375a-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="5375a-137">A `WithEvents` variable est comme toute autre variable objet.</span><span class="sxs-lookup"><span data-stu-id="5375a-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="5375a-138">Vous devez créer un objet et assigner une référence à l’aide du `WithEvents` variable.</span><span class="sxs-lookup"><span data-stu-id="5375a-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="5375a-139">Pour créer un objet et affecter une référence à celui-ci</span><span class="sxs-lookup"><span data-stu-id="5375a-139">To create an object and assign a reference to it</span></span>  
  
1.  <span data-ttu-id="5375a-140">Sélectionnez **(Form1 Events)** dans la liste déroulante dans le **éditeur de Code**.</span><span class="sxs-lookup"><span data-stu-id="5375a-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2.  <span data-ttu-id="5375a-141">Sélectionnez le `Load` événement dans la liste déroulante droite.</span><span class="sxs-lookup"><span data-stu-id="5375a-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="5375a-142">Le **éditeur de Code** ouvre le `Form1_Load` procédure événementielle.</span><span class="sxs-lookup"><span data-stu-id="5375a-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3.  <span data-ttu-id="5375a-143">Ajoutez le code suivant pour le `Form1_Load` procédure événementielle à créer le `Widget`:</span><span class="sxs-lookup"><span data-stu-id="5375a-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_4.vb)]  
  
 <span data-ttu-id="5375a-144">Lorsque ce code s’exécute, [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] crée un `Widget` de l’objet et s’y connecte les procédures d’événement associés à ses événements `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="5375a-144">When this code executes, [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="5375a-145">À partir de ce moment, chaque fois que le `Widget` déclenche son `PercentDone` événement, le `mWidget_PercentDone` procédure événementielle est exécutée.</span><span class="sxs-lookup"><span data-stu-id="5375a-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="5375a-146">Pour appeler la méthode LongTask</span><span class="sxs-lookup"><span data-stu-id="5375a-146">To call the LongTask method</span></span>  
  
-   <span data-ttu-id="5375a-147">Ajoutez le code suivant au gestionnaire d'événements `Button1_Click` :</span><span class="sxs-lookup"><span data-stu-id="5375a-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_5.vb)]  
  
 <span data-ttu-id="5375a-148">Avant du `LongTask` méthode est appelée, l’étiquette qu’affiche le pourcentage d’achèvement doit être initialisé et niveau de la classe `Boolean` indicateur pour l’annulation de la méthode doit être définie sur `False`.</span><span class="sxs-lookup"><span data-stu-id="5375a-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="5375a-149">`LongTask`est appelée avec une durée de la tâche de 12,2 secondes.</span><span class="sxs-lookup"><span data-stu-id="5375a-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="5375a-150">Le `PercentDone` événement est déclenché une fois tous les tiers de seconde.</span><span class="sxs-lookup"><span data-stu-id="5375a-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="5375a-151">Chaque fois que l’événement est déclenché, le `mWidget_PercentDone` procédure événementielle est exécutée.</span><span class="sxs-lookup"><span data-stu-id="5375a-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="5375a-152">Lorsque `LongTask` est terminé, `mblnCancel` est testé pour voir si `LongTask` s’est terminée normalement, ou si elle s’est arrêté, car `mblnCancel` a été défini sur `True`.</span><span class="sxs-lookup"><span data-stu-id="5375a-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="5375a-153">Le pourcentage d’achèvement est mis à jour uniquement dans le premier cas.</span><span class="sxs-lookup"><span data-stu-id="5375a-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="5375a-154">Pour exécuter le programme</span><span class="sxs-lookup"><span data-stu-id="5375a-154">To run the program</span></span>  
  
1.  <span data-ttu-id="5375a-155">Appuyez sur F5 pour configurer le projet en mode exécution.</span><span class="sxs-lookup"><span data-stu-id="5375a-155">Press F5 to put the project in run mode.</span></span>  
  
2.  <span data-ttu-id="5375a-156">Cliquez sur le **démarrer une tâche** bouton.</span><span class="sxs-lookup"><span data-stu-id="5375a-156">Click the **Start Task** button.</span></span> <span data-ttu-id="5375a-157">Chaque fois que le `PercentDone` événement est déclenché, l’étiquette est mis à jour avec le pourcentage de la tâche est terminée.</span><span class="sxs-lookup"><span data-stu-id="5375a-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3.  <span data-ttu-id="5375a-158">Cliquez sur le **Annuler** bouton Arrêter la tâche.</span><span class="sxs-lookup"><span data-stu-id="5375a-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="5375a-159">Notez que l’apparence de la **Annuler** bouton ne change pas immédiatement lorsque vous cliquez dessus.</span><span class="sxs-lookup"><span data-stu-id="5375a-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="5375a-160">Le `Click` événement ne peut pas se produire jusqu'à ce que la `My.Application.DoEvents` instruction permet le traitement des événements.</span><span class="sxs-lookup"><span data-stu-id="5375a-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="5375a-161">Le `My.Application.DoEvents` méthode ne traite pas les événements dans la même façon que le formulaire.</span><span class="sxs-lookup"><span data-stu-id="5375a-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="5375a-162">Par exemple, dans cette procédure pas à pas, vous devez cliquer sur le **Annuler** bouton à deux reprises.</span><span class="sxs-lookup"><span data-stu-id="5375a-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="5375a-163">Pour permettre au formulaire gérer les événements directement, vous pouvez utiliser le multithreading.</span><span class="sxs-lookup"><span data-stu-id="5375a-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="5375a-164">Pour plus d’informations, consultez [Threading](http://msdn.microsoft.com/library/552f6c68-dbdb-4327-ae36-32cf9063d88c).</span><span class="sxs-lookup"><span data-stu-id="5375a-164">For more information, see [Threading](http://msdn.microsoft.com/library/552f6c68-dbdb-4327-ae36-32cf9063d88c).</span></span>  
  
 <span data-ttu-id="5375a-165">Il peut s’avérer intéressant d’exécuter le programme en appuyant sur F11 pour exécuter le code une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="5375a-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="5375a-166">Vous pouvez voir clairement comment l’exécution passe `LongTask`et puis brièvement recommence `Form1` chaque fois que le `PercentDone` événement est déclenché.</span><span class="sxs-lookup"><span data-stu-id="5375a-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="5375a-167">Que se passerait-il si, alors que l’exécution a été dans le code de `Form1`, le `LongTask` méthode était de nouveau appelée ?</span><span class="sxs-lookup"><span data-stu-id="5375a-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="5375a-168">Au pire des cas, un débordement de pile peut se produire si `LongTask` appelées chaque fois que l’événement a été déclenché.</span><span class="sxs-lookup"><span data-stu-id="5375a-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="5375a-169">Vous pouvez provoquer la variable `mWidget` pour gérer les événements d’un autre `Widget` en assignant une référence au nouvel objet de `Widget` à `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="5375a-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="5375a-170">En fait, vous pouvez rendre le code dans `Button1_Click` cette opération chaque fois que vous cliquez sur le bouton.</span><span class="sxs-lookup"><span data-stu-id="5375a-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="5375a-171">Pour gérer les événements pour un autre objet widget</span><span class="sxs-lookup"><span data-stu-id="5375a-171">To handle events for a different widget</span></span>  
  
-   <span data-ttu-id="5375a-172">Ajoutez la ligne suivante de code pour le `Button1_Click` procédure, juste avant la ligne `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="5375a-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_6.vb)]  
  
 <span data-ttu-id="5375a-173">Le code ci-dessus crée un nouveau `Widget` chaque fois que le bouton est activé.</span><span class="sxs-lookup"><span data-stu-id="5375a-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="5375a-174">Dès que le `LongTask` méthode se termine, la référence à la `Widget` est publié et le `Widget` est détruit.</span><span class="sxs-lookup"><span data-stu-id="5375a-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="5375a-175">A `WithEvents` variable peut contenir uniquement une référence d’objet à la fois, si vous affectez une autre `Widget` à l’objet `mWidget`, précédent `Widget` événements d’objet seront n’est plus gérées.</span><span class="sxs-lookup"><span data-stu-id="5375a-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="5375a-176">Si `mWidget` est la seule variable objet qui contient une référence à l’ancien `Widget`, l’objet est détruit.</span><span class="sxs-lookup"><span data-stu-id="5375a-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="5375a-177">Si vous souhaitez gérer les événements de plusieurs `Widget` objets, utilisez la `AddHandler` instruction pour traiter les événements de chaque objet séparément.</span><span class="sxs-lookup"><span data-stu-id="5375a-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="5375a-178">Vous pouvez déclarer autant `WithEvents` les variables que vous avez besoin, mais les tableaux de `WithEvents` variables ne sont pas pris en charge.</span><span class="sxs-lookup"><span data-stu-id="5375a-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="5375a-179">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="5375a-179">See Also</span></span>  
 [<span data-ttu-id="5375a-180">Procédure pas à pas : déclaration et déclenchement des événements</span><span class="sxs-lookup"><span data-stu-id="5375a-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)  
 [<span data-ttu-id="5375a-181">Événements</span><span class="sxs-lookup"><span data-stu-id="5375a-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
