---
title: Tableaux dans Visual Basic
ms.custom: 
ms.date: 12/06/2017
ms.prod: .net
ms.technology: devlang-visual-basic
ms.topic: article
f1_keywords: vb.Array
helpviewer_keywords:
- arrays [Visual Basic]
- Visual Basic, arrays
ms.assetid: dbf29737-b589-4443-bee6-a27588d9c67e
author: rpetrusha
ms.author: ronpet
ms.manager: wpickett
ms.openlocfilehash: d223ca8b0ff59a13c31fa777e5cb6a97918421c6
ms.sourcegitcommit: 01ea3686e74ff05e4f6de3d8d46dc603d051ec00
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 12/13/2017
---
# <a name="arrays-in-visual-basic"></a><span data-ttu-id="d3584-102">Tableaux dans Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d3584-102">Arrays in Visual Basic</span></span>
<span data-ttu-id="d3584-103">Un tableau est un ensemble de valeurs, qui sont appelés *éléments*, qui sont logiquement liés entre eux.</span><span class="sxs-lookup"><span data-stu-id="d3584-103">An array is a set of values, which are termed *elements*, that are logically related to each other.</span></span> <span data-ttu-id="d3584-104">Par exemple, un tableau peut contenir le nombre d’étudiants de chaque niveau scolaire dans une école primaire ; chaque élément du tableau est le nombre d’élèves d’un niveau unique.</span><span class="sxs-lookup"><span data-stu-id="d3584-104">For example, an array may consist of the number of students in each grade in a grammar school; each element of the array is the number of students in a single grade.</span></span> <span data-ttu-id="d3584-105">De même, un tableau peut se composer de notes d’un étudiant pour une classe ; chaque élément du tableau est un niveau unique.</span><span class="sxs-lookup"><span data-stu-id="d3584-105">Similarly, an array may consist of a student's grades for a class; each element of the array is a single grade.</span></span>    

<span data-ttu-id="d3584-106">Il est possible de variables individuelles pour stocker chacune de nos éléments de données.</span><span class="sxs-lookup"><span data-stu-id="d3584-106">It is possible individual variables to store each of our data items.</span></span> <span data-ttu-id="d3584-107">Par exemple, si notre application analyse les étudiants, nous pouvons utiliser une variable distincte pour le niveau de chaque student, tel que `englishGrade1`, `englishGrade2`, etc. Cette approche présente les trois principales limitations :</span><span class="sxs-lookup"><span data-stu-id="d3584-107">For example, if our application analyzes student grades, we can use a separate variable for each student's grade, such as `englishGrade1`, `englishGrade2`, etc. This approach has three major limitations:</span></span>
- <span data-ttu-id="d3584-108">Nous devons savoir au moment du design exactement le nombre de notes que nous avons à gérer.</span><span class="sxs-lookup"><span data-stu-id="d3584-108">We have to know at design time exactly how many grades we have to handle.</span></span>
- <span data-ttu-id="d3584-109">Traiter un grand nombre de niveaux élevés rapidement devient complexe.</span><span class="sxs-lookup"><span data-stu-id="d3584-109">Handling large numbers of grades quickly becomes unwieldy.</span></span> <span data-ttu-id="d3584-110">Cela facilite ensuite une application beaucoup plus susceptible d’avoir des bogues sérieux.</span><span class="sxs-lookup"><span data-stu-id="d3584-110">This in turn makes an application much more likely to have serious bugs.</span></span>
- <span data-ttu-id="d3584-111">Il est difficile à maintenir.</span><span class="sxs-lookup"><span data-stu-id="d3584-111">It is difficult to maintain.</span></span> <span data-ttu-id="d3584-112">Chaque nouveau niveau que nous ajoutons nécessite que l’application être modifiée et recompilée et redéployée.</span><span class="sxs-lookup"><span data-stu-id="d3584-112">Each new grade that we add requires that the application be modified, recompiled, and redeployed.</span></span>  
 
 <span data-ttu-id="d3584-113">En utilisant un tableau, vous pouvez faire référence à ces valeurs connexes par le même nom et utilisez un nombre qui est appelé un *index* ou *indice* pour identifier un élément individuel en fonction de sa position dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-113">By using an array, you can refer to these related values by the same name, and use a number that’s called an *index* or *subscript* to identify an individual element based on its position in the array.</span></span> <span data-ttu-id="d3584-114">Les index d’une plage de tableau à partir de 0 à un de moins que le nombre total d’éléments dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-114">The indexes of an array range from 0 to one less than the total number of elements in the array.</span></span> <span data-ttu-id="d3584-115">Lorsque vous utilisez la syntaxe Visual Basic pour définir la taille d’un tableau, vous spécifiez ses index le plus élevé, pas le nombre total d’éléments dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-115">When you use Visual Basic syntax to define the size of an array, you specify its highest index, not the total number of elements in the array.</span></span> <span data-ttu-id="d3584-116">Vous pouvez utiliser le tableau en tant qu’unité, et la possibilité d’itérer au sein de ses éléments vous évite d’avoir à connaître exactement le nombre d’éléments qu’il contient au moment du design.</span><span class="sxs-lookup"><span data-stu-id="d3584-116">You can work with the array as a unit, and the ability to iterate its elements frees you from needing to know exactly how many elements it contains at design time.</span></span>
  
 <span data-ttu-id="d3584-117">Voici quelques exemples sommaires avant d’entrer dans les détails :</span><span class="sxs-lookup"><span data-stu-id="d3584-117">Some quick examples before explanation:</span></span>  
  
```vb  
' Declare a single-dimension array of 5 numbers.  
Dim numbers(4) As Integer   
  
'Declare a single-dimension array and set its 4 values.  
Dim numbers = New Integer() {1, 2, 4, 8}  
  
' Change the size of an existing array to 16 elements and retain the current values.
ReDim Preserve numbers(15)
  
' Redefine the size of an existing array and reset the values.
ReDim numbers(15)  
  
' Declare a 6 x 6 multidimensional array.
Dim matrix(5, 5) As Double  
  
' Declare a 4 x 3 multidimensional array and set array element values.  
Dim matrix = New Integer(3, 2) {{1, 2, 3}, {2, 3, 4}, {3, 4, 5}, {4, 5, 6}}  
  
' Declare a jagged array  
Dim sales()() As Double = New Double(11)() {}  
```  
  
 ## <a name="in-this-article"></a><span data-ttu-id="d3584-118">Dans cet article</span><span class="sxs-lookup"><span data-stu-id="d3584-118">In this article</span></span>
  
- [<span data-ttu-id="d3584-119">Éléments de tableau dans un tableau simple</span><span class="sxs-lookup"><span data-stu-id="d3584-119">Array elements in a simple array</span></span>](#array-elements-in-a-simple-array)  
  
- [<span data-ttu-id="d3584-120">Création d’un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-120">Creating an array</span></span>](#creating-an-array)  
  
- [<span data-ttu-id="d3584-121">Stocker des valeurs dans un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-121">Storing values in an array</span></span>](#storing-values-in-an-array)  
  
- [<span data-ttu-id="d3584-122">Remplissage d’un tableau avec des littéraux de tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-122">Populating an array with array literals</span></span>](#populating-an-array-with-array-literals)  
  
- [<span data-ttu-id="d3584-123">Effectuer une itération au sein d’un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-123">Iterating through an array</span></span>](#iterating-through-an-array)  
  
- [<span data-ttu-id="d3584-124">Taille du tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-124">Array size</span></span>](#BKMK_ArraySize)  

- [<span data-ttu-id="d3584-125">Le type de tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-125">The array type</span></span>](#the-array-type)  
  
- [<span data-ttu-id="d3584-126">Tableaux en tant que valeurs de retour et paramètres</span><span class="sxs-lookup"><span data-stu-id="d3584-126">Arrays as return values and parameters</span></span>](#arrays-as-return-values-and-parameters)  
- [<span data-ttu-id="d3584-127">Tableaux en escalier</span><span class="sxs-lookup"><span data-stu-id="d3584-127">Jagged arrays</span></span>](#jagged-arrays)  
  
- [<span data-ttu-id="d3584-128">Tableaux de longueur zéro</span><span class="sxs-lookup"><span data-stu-id="d3584-128">Zero-length arrays</span></span>](#zero-length-arrays)  

- [<span data-ttu-id="d3584-129">Fractionnement d’un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-129">Splitting an array</span></span>](#splitting-an-array)
  
- [<span data-ttu-id="d3584-130">Collections comme alternative aux tableaux</span><span class="sxs-lookup"><span data-stu-id="d3584-130">Collections as an alternative to arrays</span></span>](#collections-as-an-alternative-to-arrays)  
  
##  <a name="array-elements-in-a-simple-array"></a><span data-ttu-id="d3584-131">Éléments de tableau dans un tableau simple</span><span class="sxs-lookup"><span data-stu-id="d3584-131">Array elements in a simple array</span></span>  

<span data-ttu-id="d3584-132">Nous allons créer un tableau nommé `students` pour stocker le nombre d’élèves de chaque niveau scolaire dans une école primaire.</span><span class="sxs-lookup"><span data-stu-id="d3584-132">Let's create an array named `students` to store the number of students in each grade in a grammar school.</span></span> <span data-ttu-id="d3584-133">Les index des éléments s’échelonnent de 0 à 6.</span><span class="sxs-lookup"><span data-stu-id="d3584-133">The indexes of the elements range from 0 through 6.</span></span> <span data-ttu-id="d3584-134">À l’aide de ce tableau est plus simple que de déclarer sept variables.</span><span class="sxs-lookup"><span data-stu-id="d3584-134">Using this array is simpler than declaring seven variables.</span></span>

<span data-ttu-id="d3584-135">L’illustration suivante montre le `students` tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-135">The following illustration shows the `students` array.</span></span> <span data-ttu-id="d3584-136">Pour chaque élément du tableau :</span><span class="sxs-lookup"><span data-stu-id="d3584-136">For each element of the array:</span></span>  
  
-   <span data-ttu-id="d3584-137">L’index de l’élément représente le niveau scolaire (l’index 0 représente le niveau maternelle).</span><span class="sxs-lookup"><span data-stu-id="d3584-137">The index of the element represents the grade (index 0 represents kindergarten).</span></span>
  
-   <span data-ttu-id="d3584-138">La valeur contenue dans l’élément représente le nombre d’élèves de cette catégorie.</span><span class="sxs-lookup"><span data-stu-id="d3584-138">The value that’s contained in the element represents the number of students in that grade.</span></span>
  
 <span data-ttu-id="d3584-139">![Image d’un tableau montrant des nombres d’étudiants](../../../../visual-basic/programming-guide/language-features/arrays/media/arrayexampleschool.gif "ArrayExampleSchool")</span><span class="sxs-lookup"><span data-stu-id="d3584-139">![Picture of array showing numbers of students](../../../../visual-basic/programming-guide/language-features/arrays/media/arrayexampleschool.gif "ArrayExampleSchool")</span></span>  
<span data-ttu-id="d3584-140">Éléments du tableau « students » (élèves)</span><span class="sxs-lookup"><span data-stu-id="d3584-140">Elements of the "students" array</span></span>  
 
<span data-ttu-id="d3584-141">L’exemple suivant contient le code Visual Basic qui crée et utilise le tableau :</span><span class="sxs-lookup"><span data-stu-id="d3584-141">The following example contains the Visual Basic code that creates and uses the array:</span></span>

 [!code-vb[simple-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/simple-array.vb)]  

<span data-ttu-id="d3584-142">L’exemple effectue trois opérations :</span><span class="sxs-lookup"><span data-stu-id="d3584-142">The example does three things:</span></span>

- <span data-ttu-id="d3584-143">Elle déclare un `students` tableau avec sept éléments.</span><span class="sxs-lookup"><span data-stu-id="d3584-143">It declares a `students` array with seven elements.</span></span> <span data-ttu-id="d3584-144">Le nombre `6` dans le tableau de déclaration indique le dernier index de tableau ; il l’est inférieur au nombre d’éléments dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-144">The number `6` in the array declaration indicates the last index in the array; it is one less than the number of elements in the array.</span></span>
- <span data-ttu-id="d3584-145">Il assigne des valeurs à chaque élément du tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-145">It assigns values to each element in the array.</span></span> <span data-ttu-id="d3584-146">Éléments de tableau sont accessibles en utilisant le nom de tableau et en incluant l’index de l’élément individuel entre parenthèses.</span><span class="sxs-lookup"><span data-stu-id="d3584-146">Array elements are accessed by using the array name and including the index of the individual element in parentheses.</span></span>
- <span data-ttu-id="d3584-147">Il répertorie chaque valeur du tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-147">It lists each value of the array.</span></span> <span data-ttu-id="d3584-148">L’exemple utilise un [ `For` ](../../../language-reference/statements/for-next-statement.md) instruction pour accéder à chaque élément du tableau par son numéro d’index.</span><span class="sxs-lookup"><span data-stu-id="d3584-148">The example uses a [`For`](../../../language-reference/statements/for-next-statement.md) statement to access each element of the array by its index number.</span></span>
  
 <span data-ttu-id="d3584-149">Le `students` tableau dans l’exemple précédent est un tableau unidimensionnel, car elle utilise un seul index.</span><span class="sxs-lookup"><span data-stu-id="d3584-149">The `students` array in the preceding example is a one-dimensional array because it uses one index.</span></span> <span data-ttu-id="d3584-150">Un tableau qui utilise plusieurs index ou indices est appelé *multidimensionnelles*.</span><span class="sxs-lookup"><span data-stu-id="d3584-150">An array that uses more than one index or subscript is called *multidimensional*.</span></span> <span data-ttu-id="d3584-151">Pour plus d’informations, consultez le reste de cet article et [les Dimensions du tableau dans Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-151">For more information, see the rest of this article and [Array Dimensions in Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md).</span></span>  
  
##  <a name="creating-an-array"></a><span data-ttu-id="d3584-152">Création d’un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-152">Creating an Array</span></span>  
 
<span data-ttu-id="d3584-153">Vous pouvez définir la taille d’un tableau de plusieurs façons :</span><span class="sxs-lookup"><span data-stu-id="d3584-153">You can define the size of an array in several ways:</span></span> 

- <span data-ttu-id="d3584-154">Vous pouvez spécifier la taille lorsque le tableau est déclaré :</span><span class="sxs-lookup"><span data-stu-id="d3584-154">You can specify the size when the array is declared:</span></span>
  
    [!code-vb[creating1](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#1)]  
  
 - <span data-ttu-id="d3584-155">Vous pouvez utiliser un `New` clause pour indiquer la taille d’un tableau lors de sa création :</span><span class="sxs-lookup"><span data-stu-id="d3584-155">You can use a `New` clause to supply the size of an array when it’s created:</span></span>  
  
    [!code-vb[creating2](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#2)]  
  
 <span data-ttu-id="d3584-156">Si vous avez un groupe existant, vous pouvez redéfinir sa taille à l’aide de la [ `Redim` ](../../../../visual-basic/language-reference/statements/redim-statement.md) instruction.</span><span class="sxs-lookup"><span data-stu-id="d3584-156">If you have an existing array, you can redefine its size by using the [`Redim`](../../../../visual-basic/language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="d3584-157">Vous pouvez spécifier que la `Redim` instruction conserver les valeurs qui se trouvent dans le tableau, ou vous pouvez spécifier qu’elle crée un tableau vide.</span><span class="sxs-lookup"><span data-stu-id="d3584-157">You can specify that the `Redim` statement keep the values that are in the array, or you can specify that it create an empty array.</span></span> <span data-ttu-id="d3584-158">L’exemple suivant montre les différentes utilisations possibles de l’instruction `Redim` pour modifier la taille d’un tableau existant.</span><span class="sxs-lookup"><span data-stu-id="d3584-158">The following example shows different uses of the `Redim` statement to modify the size of an existing array.</span></span>  
  
 [!code-vb[redimensioning](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#3)]  
  
 <span data-ttu-id="d3584-159">Pour plus d’informations, consultez la [instruction ReDim](../../../../visual-basic/language-reference/statements/redim-statement.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-159">For more information, see the [ReDim Statement](../../../../visual-basic/language-reference/statements/redim-statement.md).</span></span>  
  
##  <a name="storing-values-in-an-array"></a><span data-ttu-id="d3584-160">Stockage de valeurs dans un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-160">Storing Values in an Array</span></span>
 
 <span data-ttu-id="d3584-161">Vous pouvez accéder à chaque emplacement d’un tableau en utilisant un index de type `Integer`.</span><span class="sxs-lookup"><span data-stu-id="d3584-161">You can access each location in an array by using an index of type `Integer`.</span></span> <span data-ttu-id="d3584-162">Vous pouvez stocker des valeurs dans un tableau et les récupérer par la suite en référençant chaque emplacement du tableau en utilisant son index entre parenthèses.</span><span class="sxs-lookup"><span data-stu-id="d3584-162">You can store and retrieve values in an array by referencing each array location by using its index enclosed in parentheses.</span></span> <span data-ttu-id="d3584-163">Index pour les tableaux multidimensionnels sont séparés par des virgules (,).</span><span class="sxs-lookup"><span data-stu-id="d3584-163">Indexes for multidimensional arrays are separated by commas (,).</span></span> <span data-ttu-id="d3584-164">Vous avez besoin d’un index pour chaque dimension de tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-164">You need one index for each array dimension.</span></span> 

<span data-ttu-id="d3584-165">L’exemple suivant montre des instructions qui stockent et récupèrent des valeurs dans des tableaux.</span><span class="sxs-lookup"><span data-stu-id="d3584-165">The following example shows some statements that store and retrieve values in arrays.</span></span>
  
 [!code-vb[store-and-retrieve](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/store-and-retrieve.vb)]  
  
## <a name="populating-an-array-with-array-literals"></a><span data-ttu-id="d3584-166">Remplissage d’un tableau avec des littéraux de tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-166">Populating an array with array literals</span></span>
 <span data-ttu-id="d3584-167">En utilisant un littéral de tableau, vous pouvez remplir un tableau avec un ensemble initial de valeurs en même temps que vous la créez.</span><span class="sxs-lookup"><span data-stu-id="d3584-167">By using an array literal, you can populate an array with an initial set of values at the same time that you create it.</span></span> <span data-ttu-id="d3584-168">Un littéral de tableau se compose d’une liste de valeurs séparées par des virgules mise entre accolades (`{}`).</span><span class="sxs-lookup"><span data-stu-id="d3584-168">An array literal consists of a list of comma-separated values that are enclosed in braces (`{}`).</span></span>  
  
 <span data-ttu-id="d3584-169">Quand vous créez un tableau en utilisant un littéral de tableau, vous pouvez soit indiquer le type du tableau, soit utiliser l’inférence de type pour déterminer le type du tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-169">When you create an array by using an array literal, you can either supply the array type or use type inference to determine the array type.</span></span> <span data-ttu-id="d3584-170">L’exemple suivant montre les deux options.</span><span class="sxs-lookup"><span data-stu-id="d3584-170">The following example shows both options.</span></span>  
  
 [!code-vb[create-with-literals](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#4)]  
  
 <span data-ttu-id="d3584-171">Lorsque vous utilisez l’inférence de type, le type du tableau est déterminé par le *type dominant* dans la liste des valeurs littérales.</span><span class="sxs-lookup"><span data-stu-id="d3584-171">When you use type inference, the type of the array is determined by the *dominant type* in the list of literal values.</span></span> <span data-ttu-id="d3584-172">Le type dominant est le type auquel tous les autres types dans le tableau peuvent s’étendre.</span><span class="sxs-lookup"><span data-stu-id="d3584-172">The dominant type is the type to which all other types in the array can widen.</span></span> <span data-ttu-id="d3584-173">Si ce type unique ne peut pas être déterminé, le type dominant est le type unique auquel tous les autres types du tableau peuvent se réduire.</span><span class="sxs-lookup"><span data-stu-id="d3584-173">If this unique type can’t be determined, the dominant type is the unique type to which all other types in the array can narrow.</span></span> <span data-ttu-id="d3584-174">Si aucun de ces types uniques ne peut être déterminé, le type dominant est `Object`.</span><span class="sxs-lookup"><span data-stu-id="d3584-174">If neither of these unique types can be determined, the dominant type is `Object`.</span></span> <span data-ttu-id="d3584-175">Par exemple, si la liste de valeurs fournie au littéral de tableau contient des valeurs de type `Integer`, `Long`et `Double`, le tableau qui en résulte est de type `Double`.</span><span class="sxs-lookup"><span data-stu-id="d3584-175">For example, if the list of values that’s supplied to the array literal contains values of type `Integer`, `Long`, and `Double`, the resulting array is of type `Double`.</span></span> <span data-ttu-id="d3584-176">Étant donné que `Integer` et `Long` s’étendent seulement à `Double`, `Double` est le type dominant.</span><span class="sxs-lookup"><span data-stu-id="d3584-176">Because `Integer` and `Long` widen only to `Double`, `Double` is the dominant type.</span></span> <span data-ttu-id="d3584-177">Pour plus d’informations, consultez [Widening and Narrowing Conversions](../../../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-177">For more information, see [Widening and Narrowing Conversions](../../../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md).</span></span> 
 
> [!NOTE] 
> <span data-ttu-id="d3584-178">Vous pouvez utiliser l’inférence de type uniquement pour les tableaux qui sont définis en tant que variables locales dans un membre de type.</span><span class="sxs-lookup"><span data-stu-id="d3584-178">You can use type inference only for arrays that are defined as local variables in a type member.</span></span> <span data-ttu-id="d3584-179">Si une définition de type explicite est absente, tableaux définis avec des littéraux de tableau au niveau de la classe sont de type `Object[]`.</span><span class="sxs-lookup"><span data-stu-id="d3584-179">If an explicit type definition is absent, arrays defined with array literals at the class level are of type `Object[]`.</span></span> <span data-ttu-id="d3584-180">Pour plus d’informations, consultez [l’inférence de type Local](../variables/local-type-inference.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-180">For more information, see [Local type inference](../variables/local-type-inference.md).</span></span> 

<span data-ttu-id="d3584-181">Notez que l’exemple précédent définit `values` sous forme de tableau de type `Double` même si les littéraux de tableau sont de type `Integer`.</span><span class="sxs-lookup"><span data-stu-id="d3584-181">Note that the previous example defines `values` as an array of type `Double` even though all the array literals are of type `Integer`.</span></span> <span data-ttu-id="d3584-182">Vous pouvez créer ce tableau, car les valeurs du littéral de tableau peuvent s’étendre `Double` valeurs.</span><span class="sxs-lookup"><span data-stu-id="d3584-182">You can create this array because the values in the array literal can widen to `Double` values.</span></span> 
  
 <span data-ttu-id="d3584-183">Vous pouvez également créer et remplir un tableau multidimensionnel à l’aide de *imbriqués de littéraux de tableau*.</span><span class="sxs-lookup"><span data-stu-id="d3584-183">You can also create and populate a multidimensional array by using *nested array literals*.</span></span> <span data-ttu-id="d3584-184">Littéraux de tableaux imbriqués doivent avoir un nombre de dimensions qui est cohérent avec le tableau obtenu.</span><span class="sxs-lookup"><span data-stu-id="d3584-184">Nested array literals must have a number of dimensions that’s consistent with the resulting array.</span></span> <span data-ttu-id="d3584-185">L’exemple suivant crée un tableau à deux dimensions d’entiers à l’aide de littéraux de tableaux imbriqués.</span><span class="sxs-lookup"><span data-stu-id="d3584-185">The following example creates a two-dimensional array of integers by using nested array literals.</span></span>  
  
 [!code-vb[nested-array-literals](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#5)]  
  
<span data-ttu-id="d3584-186">Lorsque vous utilisez des littéraux de tableaux imbriqués pour créer et remplir un tableau, une erreur se produit si le nombre d’éléments dans les littéraux de tableaux imbriqués ne correspondre pas.</span><span class="sxs-lookup"><span data-stu-id="d3584-186">When using nested array literals to create and populate an array, an error occurs if the number of elements in the nested array literals don't match.</span></span> <span data-ttu-id="d3584-187">Une erreur se produit également si vous déclarez explicitement la variable tableau d’avoir le même nombre de dimensions que les littéraux de tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-187">An error also occurs if you explicitly declare the array variable to have a different number of dimensions than the array literals.</span></span> 
  
<span data-ttu-id="d3584-188">Comme vous le faites pour les tableaux unidimensionnels, vous pouvez compter sur l’inférence de type lors de la création d’un tableau multidimensionnel avec des littéraux de tableaux imbriqués.</span><span class="sxs-lookup"><span data-stu-id="d3584-188">Just as you can for one-dimensional arrays, you can rely on type inference when creating a multidimensional array with nested array literals.</span></span> <span data-ttu-id="d3584-189">Le type déduit est le type dominant pour toutes les valeurs dans les littéraux de tableau pour le niveau d’imbrication.</span><span class="sxs-lookup"><span data-stu-id="d3584-189">The inferred type is the dominant type for all the values in all the array literals for all nesting level.</span></span> <span data-ttu-id="d3584-190">L’exemple suivant crée un tableau à deux dimensions de type `Double[,]` à partir des valeurs de type `Integer` et `Double`.</span><span class="sxs-lookup"><span data-stu-id="d3584-190">The following example creates a two-dimensional array of type `Double[,]` from values that are of type `Integer` and `Double`.</span></span>  
  
 [!code-vb[nested-type-inference](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#6)]  
  
 <span data-ttu-id="d3584-191">Pour obtenir des exemples supplémentaires, consultez [Comment : initialiser une variable tableau en Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-191">For additional examples, see [How to: Initialize an Array Variable in Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md).</span></span>  
  
##  <a name="iterating-through-an-array"></a><span data-ttu-id="d3584-192">Effectuer une itération au sein d’un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-192">Iterating through an array</span></span>  
 <span data-ttu-id="d3584-193">Lorsque vous itérez au sein d’un tableau, vous accédez à chaque élément du tableau à partir de l’index la plus basse à la plus élevée ou la plus élevée à la plus petite.</span><span class="sxs-lookup"><span data-stu-id="d3584-193">When you iterate through an array, you access each element in the array from the lowest index to the highest or from the highest to the lowest.</span></span> <span data-ttu-id="d3584-194">En règle générale, utilisez soit le [pour... L’instruction suivante](../../../../visual-basic/language-reference/statements/for-next-statement.md) ou [pour chaque... L’instruction suivante](../../../../visual-basic/language-reference/statements/for-each-next-statement.md) pour parcourir les éléments d’un tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-194">Typically, use use either the [For...Next Statement](../../../../visual-basic/language-reference/statements/for-next-statement.md) or the [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md) to iterate through the elements of an array.</span></span> <span data-ttu-id="d3584-195">Lorsque vous ne connaissez pas les limites supérieures du tableau, vous pouvez appeler la <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> méthode pour obtenir la valeur la plus élevée de l’index.</span><span class="sxs-lookup"><span data-stu-id="d3584-195">When you don't know the upper bounds of the array, you can call the <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> method to get the highest value of the index.</span></span> <span data-ttu-id="d3584-196">Bien que la valeur d’index la plus basse est presque toujours 0, vous pouvez appeler la <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> méthode pour obtenir la valeur la plus faible de l’index.</span><span class="sxs-lookup"><span data-stu-id="d3584-196">Although lowest index value is almost always 0, you can call the <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> method to get the lowest value of the index.</span></span>   
  
 <span data-ttu-id="d3584-197">L’exemple suivant effectue une itération dans un tableau unidimensionnel à l’aide de la [ `For...Next` ](../../../../visual-basic/language-reference/statements/for-next-statement.md) instruction.</span><span class="sxs-lookup"><span data-stu-id="d3584-197">The following example iterates through a one-dimensional array by using the [`For...Next`](../../../../visual-basic/language-reference/statements/for-next-statement.md) statement.</span></span> 
  
 [!code-vb[iterate-one-dimensional-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate1d.vb)]  
  
 <span data-ttu-id="d3584-198">L’exemple suivant effectue une itération dans un tableau multidimensionnel en utilisant un [ `For...Next` ](../../../../visual-basic/language-reference/statements/for-next-statement.md) instruction.</span><span class="sxs-lookup"><span data-stu-id="d3584-198">The following example iterates through a multidimensional array by using a [`For...Next`](../../../../visual-basic/language-reference/statements/for-next-statement.md) statement.</span></span> <span data-ttu-id="d3584-199">La méthode <xref:System.Array.GetUpperBound%2A> dispose d’un paramètre qui spécifie la dimension.</span><span class="sxs-lookup"><span data-stu-id="d3584-199">The <xref:System.Array.GetUpperBound%2A> method has a parameter that specifies the dimension.</span></span> <span data-ttu-id="d3584-200">`GetUpperBound(0)`Retourne l’index le plus élevé de la première dimension et `GetUpperBound(1)` retourne l’index le plus élevé de la deuxième dimension.</span><span class="sxs-lookup"><span data-stu-id="d3584-200">`GetUpperBound(0)` returns the highest index of the first dimension, and `GetUpperBound(1)` returns the highest index of the second dimension.</span></span>
  
 [!code-vb[iterate-two-dimensional-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate2d.vb)]  
  
 <span data-ttu-id="d3584-201">L’exemple suivant utilise un [For Each... L’instruction suivante](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)pour itérer un tableau unidimensionnel et un tableau à deux dimensions.</span><span class="sxs-lookup"><span data-stu-id="d3584-201">The following example uses a [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)to iterate through a one-dimensional array and a two-dimensional array.</span></span>  
  
 [!code-vb[iterate-for-each-next](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate-for-each-next.vb)]  
  
## <a name="array-size"></a><span data-ttu-id="d3584-202">Taille de tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-202">Array Size</span></span>  

 <span data-ttu-id="d3584-203">La taille d’un tableau est le produit des longueurs de toutes ses dimensions.</span><span class="sxs-lookup"><span data-stu-id="d3584-203">The size of an array is the product of the lengths of all its dimensions.</span></span> <span data-ttu-id="d3584-204">Elle représente le nombre total d’éléments actuellement contenus dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-204">It represents the total number of elements currently contained in the array.</span></span>  <span data-ttu-id="d3584-205">Par exemple, l’exemple suivant déclare un tableau unidimensionnel 2 à quatre éléments dans chaque dimension.</span><span class="sxs-lookup"><span data-stu-id="d3584-205">For example, the following example declares a 2-dimensional array with four elements in each dimension.</span></span> <span data-ttu-id="d3584-206">Comme le montre la sortie de l’exemple, la taille de la baie est 16 (ou (3 + 1) * (3 + 1).</span><span class="sxs-lookup"><span data-stu-id="d3584-206">As the output from the example shows, the array's size is 16 (or (3 + 1) * (3 + 1).</span></span>

 [!code-vb[array-size](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size.vb)]  

> [!NOTE] 
> <span data-ttu-id="d3584-207">Cette discussion de la taille du tableau ne s’applique pas aux tableaux en escalier.</span><span class="sxs-lookup"><span data-stu-id="d3584-207">This discussion of array size does not apply to jagged arrays.</span></span> <span data-ttu-id="d3584-208">Pour plus d’informations sur les tableaux en escalier et déterminer la taille d’un tableau en escalier, consultez le [tableaux en escalier](#jagged-arrays) section.</span><span class="sxs-lookup"><span data-stu-id="d3584-208">For information on jagged arrays and determining the size of a jagged array, see the [Jagged arrays](#jagged-arrays) section.</span></span>
  
  <span data-ttu-id="d3584-209">Vous pouvez trouver la taille d’un tableau en utilisant la propriété <xref:System.Array.Length%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d3584-209">You can find the size of an array by using the <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="d3584-210">Vous pouvez rechercher la longueur de chaque dimension d’un tableau multidimensionnel à l’aide de la <xref:System.Array.GetLength%2A?displayProperty=nameWithType> (méthode).</span><span class="sxs-lookup"><span data-stu-id="d3584-210">You can find the length of each dimension of a multidimensional array by using the <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>  
  
 <span data-ttu-id="d3584-211">Vous pouvez redimensionner une variable tableau en assignant un nouvel objet tableau ou en utilisant le [ `ReDim` instruction](../../../../visual-basic/language-reference/statements/redim-statement.md) instruction.</span><span class="sxs-lookup"><span data-stu-id="d3584-211">You can resize an array variable by assigning a new array object to it or by using the [`ReDim` Statement](../../../../visual-basic/language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="d3584-212">L’exemple suivant utilise la `ReDim` instruction pour passer un tableau de 100 éléments dans un tableau 51-élément.</span><span class="sxs-lookup"><span data-stu-id="d3584-212">The following example uses the `ReDim` statement to change a 100-element array to a 51-element array.</span></span>

 [!code-vb[resize-an-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size2.vb)]  
  
 <span data-ttu-id="d3584-213">Il y a plusieurs points à prendre en compte en ce qui concerne la taille d’un tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-213">There are several things to keep in mind when dealing with the size of an array.</span></span>  
  
|||  
|---|---|  
|<span data-ttu-id="d3584-214">Longueur de dimension</span><span class="sxs-lookup"><span data-stu-id="d3584-214">Dimension Length</span></span>|<span data-ttu-id="d3584-215">L’index de chaque dimension est basée sur 0, ce qui signifie qu’il varie de 0 à sa limite supérieure.</span><span class="sxs-lookup"><span data-stu-id="d3584-215">The index of each dimension is 0-based, which means it ranges from 0 to its upper bound.</span></span> <span data-ttu-id="d3584-216">Par conséquent, la longueur d’une dimension donnée est supérieure à la limite supérieure déclarée de cette dimension.</span><span class="sxs-lookup"><span data-stu-id="d3584-216">Therefore, the length of a given dimension is one greater than the declared upper bound of that dimension.</span></span>|  
|<span data-ttu-id="d3584-217">Limites de longueur</span><span class="sxs-lookup"><span data-stu-id="d3584-217">Length Limits</span></span>|<span data-ttu-id="d3584-218">La longueur de chaque dimension d’un tableau est limitée à la valeur maximale de la `Integer` type de données, qui est <xref:System.Int32.MaxValue?displayProperty=nameWithType> ou (2 ^ 31) - 1.</span><span class="sxs-lookup"><span data-stu-id="d3584-218">The length of every dimension of an array is limited to the maximum value of the `Integer` data type, which is <xref:System.Int32.MaxValue?displayProperty=nameWithType> or (2 ^ 31) - 1.</span></span> <span data-ttu-id="d3584-219">Cependant, la taille totale d’un tableau est aussi limitée par la mémoire disponible sur votre système.</span><span class="sxs-lookup"><span data-stu-id="d3584-219">However, the total size of an array is also limited by the memory available on your system.</span></span> <span data-ttu-id="d3584-220">Si vous essayez d’initialiser un tableau qui dépasse la quantité de mémoire disponible, le runtime lève une <xref:System.OutOfMemoryException>.</span><span class="sxs-lookup"><span data-stu-id="d3584-220">If you attempt to initialize an array that exceeds the amount of available memory, the runtime throws an <xref:System.OutOfMemoryException>.</span></span>|  
|<span data-ttu-id="d3584-221">Taille et taille d’élément</span><span class="sxs-lookup"><span data-stu-id="d3584-221">Size and Element Size</span></span>|<span data-ttu-id="d3584-222">La taille d’un tableau est indépendante du type de données de ses éléments.</span><span class="sxs-lookup"><span data-stu-id="d3584-222">An array's size is independent of the data type of its elements.</span></span> <span data-ttu-id="d3584-223">La taille représente toujours le nombre total d’éléments, pas le nombre d’octets qu’ils occupent dans la mémoire.</span><span class="sxs-lookup"><span data-stu-id="d3584-223">The size always represents the total number of elements, not the number of bytes that they consume in memory.</span></span>|  
|<span data-ttu-id="d3584-224">Consommation de mémoire</span><span class="sxs-lookup"><span data-stu-id="d3584-224">Memory Consumption</span></span>|<span data-ttu-id="d3584-225">Il est déconseillé de faire des hypothèses sur la façon dont un tableau est stocké en mémoire.</span><span class="sxs-lookup"><span data-stu-id="d3584-225">It is not safe to make any assumptions regarding how an array is stored in memory.</span></span> <span data-ttu-id="d3584-226">Le stockage varie selon la largeur de données de la plateforme. Par exemple, un même tableau utilise plus de mémoire sur un système 64 bits que sur un système 32 bits.</span><span class="sxs-lookup"><span data-stu-id="d3584-226">Storage varies on platforms of different data widths, so the same array can consume more memory on a 64-bit system than on a 32-bit system.</span></span> <span data-ttu-id="d3584-227">Selon la configuration du système au moment de l’initialisation d’un tableau, le Common Language Runtime (CLR) peut assigner du stockage de façon à regrouper les éléments aussi près que possible les uns des autres ou pour tous les adapter aux limites matérielles naturelles.</span><span class="sxs-lookup"><span data-stu-id="d3584-227">Depending on system configuration when you initialize an array, the common language runtime (CLR) can assign storage either to pack elements as close together as possible, or to align them all on natural hardware boundaries.</span></span> <span data-ttu-id="d3584-228">De même, un tableau nécessite un supplément de stockage pour ses informations de contrôle, un supplément qui augmente d’autant à chaque dimension ajoutée.</span><span class="sxs-lookup"><span data-stu-id="d3584-228">Also, an array requires a storage overhead for its control information, and this overhead increases with each added dimension.</span></span>|  

## <a name="the-array-type"></a><span data-ttu-id="d3584-229">Le type de tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-229">The array type</span></span> 
 <span data-ttu-id="d3584-230">Chaque tableau a un type de données, qui est différent du type de données de ses éléments.</span><span class="sxs-lookup"><span data-stu-id="d3584-230">Every array has a data type, which differs from the data type of its elements.</span></span> <span data-ttu-id="d3584-231">Le type de données varie d’un tableau à un autre.</span><span class="sxs-lookup"><span data-stu-id="d3584-231">There is no single data type for all arrays.</span></span> <span data-ttu-id="d3584-232">En effet, le type de données d’un tableau est déterminé par le nombre de dimensions (ou *rang*) qu’il possède et par le type de données de ses éléments.</span><span class="sxs-lookup"><span data-stu-id="d3584-232">Instead, the data type of an array is determined by the number of dimensions, or *rank*, of the array, and the data type of the elements in the array.</span></span> <span data-ttu-id="d3584-233">Deux variables tableau sont les mêmes données que si elles ont le même rang de type et leurs éléments ont les mêmes données de type.</span><span class="sxs-lookup"><span data-stu-id="d3584-233">Two array variables are of the same data type only when they have the same rank and their elements have the same data type.</span></span> <span data-ttu-id="d3584-234">Les longueurs des dimensions d’un tableau n’influencent pas le type de données de tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-234">The lengths of the dimensions of an array do not influence the array data type.</span></span>  
  
 <span data-ttu-id="d3584-235">Chaque tableau hérite de la classe <xref:System.Array?displayProperty=nameWithType> et vous pouvez déclarer une variable de type `Array`, mais vous ne pouvez pas créer un tableau de type `Array`.</span><span class="sxs-lookup"><span data-stu-id="d3584-235">Every array inherits from the <xref:System.Array?displayProperty=nameWithType> class, and you can declare a variable to be of type `Array`, but you cannot create an array of type `Array`.</span></span> <span data-ttu-id="d3584-236">Par exemple, bien que le code suivant déclare la `arr` variable qui doit être de type `Array` et appelle le <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> méthode pour instancier le tableau, le type du tableau s’avère Object [].</span><span class="sxs-lookup"><span data-stu-id="d3584-236">For example, although the following code declares the `arr` variable to be of type `Array` and calls the <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> method to instantiate the array, the array's type proves to be Object[].</span></span>

 [!code-vb[array-class](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-class.vb)] 

<span data-ttu-id="d3584-237">De même, [l’instruction ReDim](../../../../visual-basic/language-reference/statements/redim-statement.md) ne peut pas opérer sur une variable déclarée comme étant du type `Array`.</span><span class="sxs-lookup"><span data-stu-id="d3584-237">Also, the [ReDim Statement](../../../../visual-basic/language-reference/statements/redim-statement.md) cannot operate on a variable declared as type `Array`.</span></span> <span data-ttu-id="d3584-238">Pour ces raisons et pour la sécurité de type, il est recommandé de déclarer chaque tableau comme un type spécifique.</span><span class="sxs-lookup"><span data-stu-id="d3584-238">For these reasons, and for type safety, it is advisable to declare every array as a specific type.</span></span>  
  
 <span data-ttu-id="d3584-239">Vous pouvez déterminer le type de données d’un tableau ou de ses éléments de plusieurs façons.</span><span class="sxs-lookup"><span data-stu-id="d3584-239">You can find out the data type of either an array or its elements in several ways.</span></span> 
  
-   <span data-ttu-id="d3584-240">Vous pouvez appeler la <xref:System.Object.GetType%2A> méthode sur la variable pour obtenir un <xref:System.Type> objet qui représente le type au moment de l’exécution de la variable.</span><span class="sxs-lookup"><span data-stu-id="d3584-240">You can call the <xref:System.Object.GetType%2A> method on the variable to get a <xref:System.Type> object that represents the run-time type of the variable.</span></span> <span data-ttu-id="d3584-241">L’objet <xref:System.Type> contient des informations complètes dans ses propriétés et méthodes.</span><span class="sxs-lookup"><span data-stu-id="d3584-241">The <xref:System.Type> object holds extensive information in its properties and methods.</span></span>  
  
-   <span data-ttu-id="d3584-242">Vous pouvez passer la variable à la <xref:Microsoft.VisualBasic.Information.TypeName%2A> fonction permettant d’obtenir un `String` avec le nom de type au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="d3584-242">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.TypeName%2A> function to get a `String` with the name of run-time type.</span></span>  
  
 <span data-ttu-id="d3584-243">L’exemple suivant appelle les deux le `GetType` (méthode) et le `TypeName` afin de déterminer le type d’un tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-243">The following example calls the both the `GetType` method and the `TypeName` function to determine the type of an array.</span></span> <span data-ttu-id="d3584-244">Le type de tableau est `Byte(,)`.</span><span class="sxs-lookup"><span data-stu-id="d3584-244">The array type is `Byte(,)`.</span></span> <span data-ttu-id="d3584-245">Notez que la <xref:System.Type.BaseType%2A?displayProperty=nameWithType> propriété indique également que le type de base du tableau d’octets est la <xref:System.Array> classe.</span><span class="sxs-lookup"><span data-stu-id="d3584-245">Note that the <xref:System.Type.BaseType%2A?displayProperty=nameWithType> property also indicates that the base type of the byte array is the <xref:System.Array> class.</span></span>  
  
 [!code-vb[array-type](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-type.vb)]  
  
##  <a name="arrays-as-return-values-and-parameters"></a><span data-ttu-id="d3584-246">Tableaux en tant que valeurs de retour et paramètres</span><span class="sxs-lookup"><span data-stu-id="d3584-246">Arrays as return values and parameters</span></span>  
 <span data-ttu-id="d3584-247">Pour retourner un tableau à partir d’une procédure `Function`, spécifiez le type de données du tableau et le nombre de dimensions en tant que type de retour de [l’instruction Function](../../../../visual-basic/language-reference/statements/function-statement.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-247">To return an array from a `Function` procedure, specify the array data type and the number of dimensions as the return type of the [Function Statement](../../../../visual-basic/language-reference/statements/function-statement.md).</span></span> <span data-ttu-id="d3584-248">Dans la fonction, déclarez une variable tableau locale avec le même type de données et le même nombre de dimensions.</span><span class="sxs-lookup"><span data-stu-id="d3584-248">Within the function, declare a local array variable with same data type and number of dimensions.</span></span> <span data-ttu-id="d3584-249">Dans [l’instruction Return](../../../../visual-basic/language-reference/statements/return-statement.md), incluez la variable tableau locale sans parenthèses.</span><span class="sxs-lookup"><span data-stu-id="d3584-249">In the [Return Statement](../../../../visual-basic/language-reference/statements/return-statement.md), include the local array variable without parentheses.</span></span>  
  
 <span data-ttu-id="d3584-250">Pour spécifier un tableau en tant que paramètre d’une procédure `Sub` ou `Function` , définissez le paramètre en tant que tableau avec un type de données et un nombre de dimensions spécifiés.</span><span class="sxs-lookup"><span data-stu-id="d3584-250">To specify an array as a parameter to a `Sub` or `Function` procedure, define the parameter as an array with a specified data type and number of dimensions.</span></span> <span data-ttu-id="d3584-251">Dans l’appel à la procédure, passez une variable de tableau avec le même type de données et le nombre de dimensions.</span><span class="sxs-lookup"><span data-stu-id="d3584-251">In the call to the procedure, pass an array variable with the same data type and number of dimensions.</span></span>  
  
 <span data-ttu-id="d3584-252">Dans l’exemple suivant, la `GetNumbers` fonction renvoie une `Integer()`, un tableau unidimensionnel de type `Integer`.</span><span class="sxs-lookup"><span data-stu-id="d3584-252">In the following example, the `GetNumbers` function returns an `Integer()`, a one-dimensional array of type `Integer`.</span></span> <span data-ttu-id="d3584-253">La procédure `ShowNumbers` accepte un argument `Integer()` .</span><span class="sxs-lookup"><span data-stu-id="d3584-253">The `ShowNumbers` procedure accepts an `Integer()` argument.</span></span> 
  
 [!code-vb[return-value-and-params](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params.vb)]  
  
 <span data-ttu-id="d3584-254">Dans l’exemple suivant, la `GetNumbersMultiDim` fonction renvoie une `Integer(,)`, un tableau à deux dimensions de type `Integer`.</span><span class="sxs-lookup"><span data-stu-id="d3584-254">In the following example, the `GetNumbersMultiDim` function returns an `Integer(,)`, a two-dimensional array of type `Integer`.</span></span>  <span data-ttu-id="d3584-255">La procédure `ShowNumbersMultiDim` accepte un argument `Integer(,)` .</span><span class="sxs-lookup"><span data-stu-id="d3584-255">The `ShowNumbersMultiDim` procedure accepts an `Integer(,)` argument.</span></span>  
  
 [!code-vb[multidimensional-return-value](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params-2d.vb)]  
  
## <a name="jagged-arrays"></a><span data-ttu-id="d3584-256">Tableaux en escalier</span><span class="sxs-lookup"><span data-stu-id="d3584-256">Jagged Arrays</span></span>  
 
<span data-ttu-id="d3584-257">Parfois, la structure de données de votre application est à deux dimensions, mais pas rectangulaire.</span><span class="sxs-lookup"><span data-stu-id="d3584-257">Sometimes the data structure in your application is two-dimensional but not rectangular.</span></span> <span data-ttu-id="d3584-258">Par exemple, vous pouvez utiliser un tableau pour stocker les données relatives à la température maximale de chaque jour du mois.</span><span class="sxs-lookup"><span data-stu-id="d3584-258">For example, you might use an array to store data about the high temperature of each day of the month.</span></span> <span data-ttu-id="d3584-259">La première dimension du tableau représente le mois, mais la deuxième dimension représente le nombre de jours, et le nombre de jours dans un mois n’est pas uniform.</span><span class="sxs-lookup"><span data-stu-id="d3584-259">The first dimension of the array represents the month, but the second dimension represents the number of days, and the number of days in a month is not uniform.</span></span> <span data-ttu-id="d3584-260">A *tableau en escalier*, qui est également appelé un *tableau de tableaux*, est conçu pour de tels scénarios.</span><span class="sxs-lookup"><span data-stu-id="d3584-260">A *jagged array*, which is also called an *array of arrays*, is designed for such scenarios.</span></span> <span data-ttu-id="d3584-261">Un tableau en escalier est un tableau dont les éléments sont également des tableaux.</span><span class="sxs-lookup"><span data-stu-id="d3584-261">A jagged array is an array whose elements are also arrays.</span></span> <span data-ttu-id="d3584-262">Un tableau en escalier et chaque élément qu’il contient peut avoir une ou plusieurs dimensions.</span><span class="sxs-lookup"><span data-stu-id="d3584-262">A jagged array and each element in a jagged array can have one or more dimensions.</span></span>  
  
 <span data-ttu-id="d3584-263">L’exemple suivant utilise un tableau de mois, dont chaque élément est un tableau de jours.</span><span class="sxs-lookup"><span data-stu-id="d3584-263">The following example uses an array of months, each element of which is an array of days.</span></span> <span data-ttu-id="d3584-264">L’exemple utilise un tableau en escalier, car des mois différents ont le même nombre de jours.</span><span class="sxs-lookup"><span data-stu-id="d3584-264">The example uses a jagged array because different months have different numbers of days.</span></span>  <span data-ttu-id="d3584-265">L’exemple montre comment créer un tableau en escalier, affecter des valeurs et récupérer et afficher ses valeurs.</span><span class="sxs-lookup"><span data-stu-id="d3584-265">The example shows how to create a jagged array, assign values to it, and retrieve and display its values.</span></span>
  
 [!code-vb[jagged-arrays](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged.vb)]  

<span data-ttu-id="d3584-266">L’exemple précédent assigne des valeurs dans le tableau en escalier sur une base de l’élément par élément, en utilisant un `For...Next` boucle.</span><span class="sxs-lookup"><span data-stu-id="d3584-266">The previous example assigns values to the jagged array on an element-by-element basis by using a `For...Next` loop.</span></span> <span data-ttu-id="d3584-267">Vous pouvez également assigner des valeurs aux éléments d’un tableau en escalier à l’aide de littéraux de tableaux imbriqués.</span><span class="sxs-lookup"><span data-stu-id="d3584-267">You can also assign values to the elements of a jagged array by using nested array literals.</span></span> <span data-ttu-id="d3584-268">Toutefois, la tentative d’utilisation imbriquée littéraux de tableau (par exemple, ```Dim valuesjagged = {{1, 2}, {2, 3, 4}}```) génère l’erreur du compilateur [BC30568](../../../,,/../misc/bc30568.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-268">However, the attempt to use nested array literals (for example, ```Dim valuesjagged = {{1, 2}, {2, 3, 4}}```) generates compiler error [BC30568](../../../,,/../misc/bc30568.md).</span></span> <span data-ttu-id="d3584-269">Pour corriger l’erreur, placez les littéraux de tableau internes entre parenthèses.</span><span class="sxs-lookup"><span data-stu-id="d3584-269">To correct the error, enclose the inner array literals in parentheses.</span></span> <span data-ttu-id="d3584-270">Les parenthèses forcent l’expression de littéral de tableau doit être évaluée, et les valeurs résultantes sont utilisées avec le littéral de tableau externe, comme le montre l’exemple suivant.</span><span class="sxs-lookup"><span data-stu-id="d3584-270">The parentheses force the array literal expression to be evaluated, and the resulting values are used with the outer array literal, as the following example shows.</span></span>  
  
 [!code-vb[jagged-array-initialization](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-assign.vb)] 

<span data-ttu-id="d3584-271">Un tableau en escalier est un tableau unidimensionnel dont les éléments contiennent des tableaux.</span><span class="sxs-lookup"><span data-stu-id="d3584-271">A jagged array is a one-dimensional array whose elements contain arrays.</span></span> <span data-ttu-id="d3584-272">Par conséquent, le <xref:System.Array.Length%2A?displayProperty=nameWithType> propriété et la `Array.GetLength(0)` méthode retourne le nombre d’éléments dans le tableau unidimensionnel, et `Array.GetLength(1)` lève une <xref:System.IndexOutOfRangeException> , car un tableau en escalier n’est pas multidimensionnel.</span><span class="sxs-lookup"><span data-stu-id="d3584-272">Therefore, the <xref:System.Array.Length%2A?displayProperty=nameWithType> property and the `Array.GetLength(0)` method return the number of elements in the one-dimensional array, and `Array.GetLength(1)` throws an <xref:System.IndexOutOfRangeException> because a jagged array is not multidimensional.</span></span> <span data-ttu-id="d3584-273">Vous déterminez le nombre d’éléments dans chaque sous-tableau en récupérant la valeur du chaque sous-tableau <xref:System.Array.Length%2A?displayProperty=nameWithType> propriété.</span><span class="sxs-lookup"><span data-stu-id="d3584-273">You determine the number of elements in each subarray by retrieving the value of each subarray's <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="d3584-274">L’exemple suivant illustre comment déterminer le nombre d’éléments dans un tableau en escalier.</span><span class="sxs-lookup"><span data-stu-id="d3584-274">The following example illustrates how to determine the number of elements in a jagged array.</span></span>

[!code-vb[jagged-array-size](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-length.vb)] 

## <a name="zero-length-arrays"></a><span data-ttu-id="d3584-275">Tableaux de longueur zéro</span><span class="sxs-lookup"><span data-stu-id="d3584-275">Zero-length arrays</span></span>  
<span data-ttu-id="d3584-276">Visual Basic fait la distinction entre un tableau sans être initialisé (un tableau dont la valeur est `Nothing`) et un *tableau de longueur zéro* ou tableau vide (un tableau qui ne comporte aucun élément.) Un tableau non initialisé est une tâche qui n’a pas été dimensionnée ou avaient des valeurs affecté à ce dernier.</span><span class="sxs-lookup"><span data-stu-id="d3584-276">Visual Basic differentiates between a uninitialized array (an array whose value is `Nothing`) and a *zero-length array* or empty array (an array that has no elements.) An uninitialized array is one that has not been dimensioned or had any values assigned to it.</span></span> <span data-ttu-id="d3584-277">Exemple :</span><span class="sxs-lookup"><span data-stu-id="d3584-277">For example:</span></span>

```vb
Dim arr() As String
```
<span data-ttu-id="d3584-278">Un tableau de longueur zéro est déclaré avec une dimension de -1.</span><span class="sxs-lookup"><span data-stu-id="d3584-278">A zero-length array is declared with a dimension of -1.</span></span> <span data-ttu-id="d3584-279">Exemple :</span><span class="sxs-lookup"><span data-stu-id="d3584-279">For example:</span></span>

```vb
Dim arrZ(-1) As String
```
<span data-ttu-id="d3584-280">Vous pouvez être amené à créer un tableau de longueur zéro dans les cas suivants :</span><span class="sxs-lookup"><span data-stu-id="d3584-280">You might need to create a zero-length array under the following circumstances:</span></span>  
  
-   <span data-ttu-id="d3584-281">Sans risquer une <xref:System.NullReferenceException> exception, votre code doit accéder aux membres de la <xref:System.Array> class, telle que <xref:System.Array.Length%2A> ou <xref:System.Array.Rank%2A>, ou appeler une fonction Visual Basic comme <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span><span class="sxs-lookup"><span data-stu-id="d3584-281">Without risking a <xref:System.NullReferenceException> exception, your code must access members of the <xref:System.Array> class, such as <xref:System.Array.Length%2A> or <xref:System.Array.Rank%2A>, or call a Visual Basic function such as <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span></span>  
  
-   <span data-ttu-id="d3584-282">Vous souhaitez conserver le votre code simple sans avoir à vérifier pour `Nothing` comme un cas particulier.</span><span class="sxs-lookup"><span data-stu-id="d3584-282">You want to keep the your code simple by not having to check for `Nothing` as a special case.</span></span>  
  
-   <span data-ttu-id="d3584-283">Votre code interagit avec une interface de programmation d’applications (API) qui vous oblige à passer un tableau de longueur zéro à une ou plusieurs procédures ou qui retourne un tableau de longueur zéro à partir d’une ou plusieurs procédures.</span><span class="sxs-lookup"><span data-stu-id="d3584-283">Your code interacts with an application programming interface (API) that either requires you to pass a zero-length array to one or more procedures or returns a zero-length array from one or more procedures.</span></span>

## <a name="splitting-an-array"></a><span data-ttu-id="d3584-284">Fractionnement d’un tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-284">Splitting an array</span></span>

<span data-ttu-id="d3584-285">Dans certains cas, vous devrez peut-être à fractionner un tableau en plusieurs groupes.</span><span class="sxs-lookup"><span data-stu-id="d3584-285">In some cases, you may need to split a single array into multiple arrays.</span></span> <span data-ttu-id="d3584-286">Cela implique identifiant l’ou les points à laquelle le tableau doit être divisée et puis cracher du tableau en deux ou plusieurs tableaux distincts.</span><span class="sxs-lookup"><span data-stu-id="d3584-286">This involves identifying the point or points at which the array is to be split, and then spitting the array into two or more separate arrays.</span></span> 

> [!NOTE] 
> <span data-ttu-id="d3584-287">Cette section ne traite pas le fractionnement d’une chaîne unique dans un tableau de chaînes en fonction de certains délimiteur.</span><span class="sxs-lookup"><span data-stu-id="d3584-287">This section does not discuss splitting a single string into a string array based on some delimiter.</span></span> <span data-ttu-id="d3584-288">Pour plus d’informations sur le fractionnement d’une chaîne, consultez la <xref:System.String.Split%2A?displayProperty=nameWithType> (méthode).</span><span class="sxs-lookup"><span data-stu-id="d3584-288">For information on splitting a string, see the <xref:System.String.Split%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="d3584-289">Les critères courants pour fractionner un tableau sont :</span><span class="sxs-lookup"><span data-stu-id="d3584-289">The most common criteria for splitting an array are:</span></span>

- <span data-ttu-id="d3584-290">Nombre d’éléments dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-290">The number of elements in the array.</span></span> <span data-ttu-id="d3584-291">Par exemple, vous pouvez souhaiter diviser un tableau de plus d’un nombre spécifié d’éléments en un nombre de parties approximativement égales.</span><span class="sxs-lookup"><span data-stu-id="d3584-291">For example, you might want to split an array of more than a specified number of elements into a number of approximately equal parts.</span></span> <span data-ttu-id="d3584-292">Pour cela, vous pouvez utiliser la valeur retournée par une le <xref:System.Array.Length%2A?displayProperty=nameWithType> ou <xref:System.Array.GetLength%2A?displayProperty=nameWithType> (méthode).</span><span class="sxs-lookup"><span data-stu-id="d3584-292">For this purpose, you can use the value returned by either the <xref:System.Array.Length%2A?displayProperty=nameWithType> or <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

- <span data-ttu-id="d3584-293">La valeur d’un élément, qui sert d’un délimiteur qui indique où le tableau doit être fractionné.</span><span class="sxs-lookup"><span data-stu-id="d3584-293">The value of an element, which serves as a delimiter that indicates where the array should be split.</span></span> <span data-ttu-id="d3584-294">Vous pouvez rechercher une valeur spécifique en appelant le <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> et <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> méthodes.</span><span class="sxs-lookup"><span data-stu-id="d3584-294">You can search for a specific value by calling the <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> and <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> methods.</span></span>
 
<span data-ttu-id="d3584-295">Une fois que vous avez déterminé l’ou les index à laquelle le tableau doit être fractionné, vous pouvez ensuite créer les groupes individuels en appelant le <xref:System.Array.Copy%2A?displayProperty=nameWithType> (méthode).</span><span class="sxs-lookup"><span data-stu-id="d3584-295">Once you've determined the index or indexes at which the array should be split, you can then create the individual arrays by calling the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span> 

<span data-ttu-id="d3584-296">L’exemple suivant fractionne un tableau en deux tableaux d’environ de taille égale.</span><span class="sxs-lookup"><span data-stu-id="d3584-296">The following example splits an array into two arrays of approximately equal size.</span></span> <span data-ttu-id="d3584-297">(Si le nombre total d’éléments de tableau est impair, le premier tableau a un élément de plus que la seconde).</span><span class="sxs-lookup"><span data-stu-id="d3584-297">(If the total number of array elements is odd, the first array has one more element than the second.)</span></span> 

[!code-vb[splitting-an-array-by-length](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/split1.vb)] 

<span data-ttu-id="d3584-298">L’exemple suivant fractionne un tableau de chaînes dans les deux tableaux en fonction de la présence d’un élément dont la valeur est « zzz », qui sert de délimiteur de tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-298">The following example splits a string array into two arrays based on the presence of an element whose value is "zzz", which serves as the array delimiter.</span></span> <span data-ttu-id="d3584-299">Les nouveaux tableaux n’incluent pas l’élément qui contient le délimiteur.</span><span class="sxs-lookup"><span data-stu-id="d3584-299">The new arrays do not include the element that contains the delimiter.</span></span>

[!code-vb[splitting-an-array-by-delimiter](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/split2.vb)] 

## <a name="joining-arrays"></a><span data-ttu-id="d3584-300">Jointure de tableaux</span><span class="sxs-lookup"><span data-stu-id="d3584-300">Joining arrays</span></span>

<span data-ttu-id="d3584-301">Vous pouvez également combiner un nombre de groupes dans un seul tableau supérieure.</span><span class="sxs-lookup"><span data-stu-id="d3584-301">You can also combine a number of arrays into a single larger array.</span></span> <span data-ttu-id="d3584-302">Pour ce faire, vous utilisez également le <xref:System.Array.Copy%2A?displayProperty=nameWithType> (méthode).</span><span class="sxs-lookup"><span data-stu-id="d3584-302">To do this, you also use the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span> 

> [!NOTE] 
> <span data-ttu-id="d3584-303">Cette section ne traite pas de joindre un tableau de chaînes en une seule chaîne.</span><span class="sxs-lookup"><span data-stu-id="d3584-303">This section does not discuss joining a string array into a single string.</span></span> <span data-ttu-id="d3584-304">Pour plus d’informations sur la jonction d’un tableau de chaînes, consultez le <xref:System.String.Join%2A?displayProperty=nameWithType> (méthode).</span><span class="sxs-lookup"><span data-stu-id="d3584-304">For information on joining a string array, see the <xref:System.String.Join%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="d3584-305">Avant de copier les éléments de chaque tableau dans le nouveau tableau, vous devez d’abord vous assurer que vous avez initialisé le tableau afin qu’il soit suffisamment grand pour accompodate le nouveau tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-305">Before copying the elements of each array into the new array, you must first ensure that you have initialized the array so that it is large enough to accompodate the new array.</span></span> <span data-ttu-id="d3584-306">Vous pouvez le faire de deux façons :</span><span class="sxs-lookup"><span data-stu-id="d3584-306">You can do this in one of two ways:</span></span>

- <span data-ttu-id="d3584-307">Utilisez le [ `ReDim Preserve` ](../../../../visual-basic/language-reference/statements/redim-statement.md) instruction à l’expansion dynamique du tableau avant d’ajouter de nouveaux éléments à celui-ci.</span><span class="sxs-lookup"><span data-stu-id="d3584-307">Use the [`ReDim Preserve`](../../../../visual-basic/language-reference/statements/redim-statement.md) statement to dynamically expand the array before adding new elements to it.</span></span> <span data-ttu-id="d3584-308">C’est la technique la plus simple, mais elle peut entraîner une dégradation des performances et la consommation excessive de la mémoire lorsque vous copiez des tableaux volumineux.</span><span class="sxs-lookup"><span data-stu-id="d3584-308">This is the easiest technique, but it can result in performance degradation and excessive memory consumption when you are copying large arrays.</span></span>
- <span data-ttu-id="d3584-309">Calculer le nombre total d’éléments nécessaires pour le nouveau tableau de grande taille, puis ajoutez les éléments de chaque tableau source à elle.</span><span class="sxs-lookup"><span data-stu-id="d3584-309">Calculate the total number of elements needed for the new large array, then add the elements of each source array to it.</span></span>

<span data-ttu-id="d3584-310">L’exemple suivant utilise la deuxième approche pour ajouter les quatre tableaux avec dix éléments à un tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-310">The following example uses the second approach to add four arrays with ten elements each to a single array.</span></span>  

[!code-vb[joining-an-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/join.vb)] 

<span data-ttu-id="d3584-311">Étant donné que dans ce cas, les tableaux source sont tous les petits, nous pouvons également dynamiquement développer le tableau quand nous ajouterons les éléments de chaque nouveau tableau à celui-ci.</span><span class="sxs-lookup"><span data-stu-id="d3584-311">Since in this case the source arrays are all small, we can also dynamically expand the array as we add the elements of each new array to it.</span></span> <span data-ttu-id="d3584-312">L’exemple suivant effectue cette opération.</span><span class="sxs-lookup"><span data-stu-id="d3584-312">The following example does that.</span></span>

[!code-vb[joining-an-array-dynamically](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/join2.vb)] 

##  <a name="collections-as-an-alternative-to-arrays"></a><span data-ttu-id="d3584-313">Collections comme alternative aux tableaux</span><span class="sxs-lookup"><span data-stu-id="d3584-313">Collections as an alternative to arrays</span></span>  
 <span data-ttu-id="d3584-314">Les tableaux s’avèrent particulièrement utiles pour créer et utiliser un nombre fixe d’objets fortement typés.</span><span class="sxs-lookup"><span data-stu-id="d3584-314">Arrays are most useful for creating and working with a fixed number of strongly typed objects.</span></span> <span data-ttu-id="d3584-315">Les collections offrent plus de souplesse quand il s’agit d’utiliser des groupes d’objets.</span><span class="sxs-lookup"><span data-stu-id="d3584-315">Collections provide a more flexible way to work with groups of objects.</span></span> <span data-ttu-id="d3584-316">Contrairement aux tableaux, qui requiert que vous modifiez explicitement la taille d’un tableau avec la [ `ReDim` instruction](../../../../visual-basic/language-reference/statements/redim-statement.md), collections de croître et réduire dynamiquement en fonction des besoins d’un changement de l’application.</span><span class="sxs-lookup"><span data-stu-id="d3584-316">Unlike arrays, which require that you explicitly change the size of an array with the [`ReDim` Statement](../../../../visual-basic/language-reference/statements/redim-statement.md), collections grow and shrink dynamically as the needs of an application change.</span></span>  
  
 <span data-ttu-id="d3584-317">Lorsque vous utilisez `ReDim` pour redimensionner un tableau, Visual Basic crée un tableau et libère le précédent.</span><span class="sxs-lookup"><span data-stu-id="d3584-317">When you use `ReDim` to redimension an array, Visual Basic creates a new array and releases the previous one.</span></span> <span data-ttu-id="d3584-318">Cela prend du temps d’exécution.</span><span class="sxs-lookup"><span data-stu-id="d3584-318">This takes execution time.</span></span> <span data-ttu-id="d3584-319">Par conséquent, si le nombre d’éléments que vous utilisez change régulièrement ou si vous ne pouvez pas prédire le nombre maximal d’éléments dont vous avez besoin, vous allez obtenir généralement de meilleures performances à l’aide d’une collection.</span><span class="sxs-lookup"><span data-stu-id="d3584-319">Therefore, if the number of items you are working with changes frequently, or you cannot predict the maximum number of items you need, you'll usually obtain better performance by using a collection.</span></span>  
  
 <span data-ttu-id="d3584-320">Pour certaines collections, vous pouvez assigner une clé à un objet que vous placez dans la collection pour vous permettre de récupérer rapidement l’objet à l’aide de la clé.</span><span class="sxs-lookup"><span data-stu-id="d3584-320">For some collections, you can assign a key to any object that you put into the collection so that you can quickly retrieve the object by using the key.</span></span>  
  
 <span data-ttu-id="d3584-321">Si votre collection contient des éléments d’un seul type de données, vous pouvez utiliser l’une des classes de l’espace de noms <xref:System.Collections.Generic?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="d3584-321">If your collection contains elements of only one data type, you can use one of the classes in the <xref:System.Collections.Generic?displayProperty=nameWithType> namespace.</span></span> <span data-ttu-id="d3584-322">Une collection générique applique la cohérence des types pour éviter qu’un autre type puisse y être ajouté.</span><span class="sxs-lookup"><span data-stu-id="d3584-322">A generic collection enforces type safety so that no other data type can be added to it.</span></span>  
  
 <span data-ttu-id="d3584-323">Pour plus d’informations sur les collections, consultez [Collections](../../concepts/collections.md).</span><span class="sxs-lookup"><span data-stu-id="d3584-323">For more information about collections, see [Collections](../../concepts/collections.md).</span></span>
  
## <a name="related-topics"></a><span data-ttu-id="d3584-324">Rubriques connexes</span><span class="sxs-lookup"><span data-stu-id="d3584-324">Related Topics</span></span>  
  
|<span data-ttu-id="d3584-325">Terme</span><span class="sxs-lookup"><span data-stu-id="d3584-325">Term</span></span>|<span data-ttu-id="d3584-326">Définition</span><span class="sxs-lookup"><span data-stu-id="d3584-326">Definition</span></span>|  
|----------|----------------|  
|[<span data-ttu-id="d3584-327">Array Dimensions in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d3584-327">Array Dimensions in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md)|<span data-ttu-id="d3584-328">Explique le rang et les dimensions des tableaux.</span><span class="sxs-lookup"><span data-stu-id="d3584-328">Explains rank and dimensions in arrays.</span></span>|  
|[<span data-ttu-id="d3584-329">Comment : initialiser une variable tableau en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d3584-329">How to: Initialize an Array Variable in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md)|<span data-ttu-id="d3584-330">Explique comment remplir les tableaux de valeurs initiales.</span><span class="sxs-lookup"><span data-stu-id="d3584-330">Describes how to populate arrays with initial values.</span></span>|  
|[<span data-ttu-id="d3584-331">Comment : trier un tableau dans Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d3584-331">How to: Sort An Array in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-sort-an-array.md)|<span data-ttu-id="d3584-332">Montre comment trier les éléments d’un tableau par ordre alphabétique.</span><span class="sxs-lookup"><span data-stu-id="d3584-332">Shows how to sort the elements of an array alphabetically.</span></span>|  
|[<span data-ttu-id="d3584-333">Guide pratique : assigner un tableau à un autre tableau</span><span class="sxs-lookup"><span data-stu-id="d3584-333">How to: Assign One Array to Another Array</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-assign-one-array-to-another-array.md)|<span data-ttu-id="d3584-334">Décrit les règles et les étapes à suivre pour assigner un tableau à une autre variable tableau.</span><span class="sxs-lookup"><span data-stu-id="d3584-334">Describes the rules and steps for assigning an array to another array variable.</span></span>|  
|[<span data-ttu-id="d3584-335">Dépannage des tableaux</span><span class="sxs-lookup"><span data-stu-id="d3584-335">Troubleshooting Arrays</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/troubleshooting-arrays.md)|<span data-ttu-id="d3584-336">Décrit des problèmes courants qui surviennent dans le cadre de l’utilisation de tableaux.</span><span class="sxs-lookup"><span data-stu-id="d3584-336">Discusses some common problems that arise when working with arrays.</span></span>|  
  
## <a name="see-also"></a><span data-ttu-id="d3584-337">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="d3584-337">See Also</span></span>  
 <xref:System.Array?displayProperty=nameWithType>  
 [<span data-ttu-id="d3584-338">Dim (instruction)</span><span class="sxs-lookup"><span data-stu-id="d3584-338">Dim Statement</span></span>](../../../../visual-basic/language-reference/statements/dim-statement.md)  
 [<span data-ttu-id="d3584-339">ReDim (instruction)</span><span class="sxs-lookup"><span data-stu-id="d3584-339">ReDim Statement</span></span>](../../../../visual-basic/language-reference/statements/redim-statement.md)
