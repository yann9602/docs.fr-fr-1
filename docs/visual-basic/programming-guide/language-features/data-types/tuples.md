---
title: Tuples dans Visual Basic
ms.custom: 
ms.date: 04/23/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords: tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: be50b22e9acca9ff8cfbde798d78869ee1c72634
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 10/18/2017
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="60296-102">Tuples (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="60296-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="60296-103">À partir de Visual Basic 2017, du langage Visual Basic propose une prise en charge intégrée pour les tuples qui permet de créer des tuples et accéder aux éléments de tuples plus facile.</span><span class="sxs-lookup"><span data-stu-id="60296-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="60296-104">Un tuple est une structure de données de légers qui a un nombre spécifique et une séquence de valeurs.</span><span class="sxs-lookup"><span data-stu-id="60296-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="60296-105">Lorsque vous instanciez le tuple, vous définissez le nombre et le type de données de chaque valeur (ou élément).</span><span class="sxs-lookup"><span data-stu-id="60296-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="60296-106">Par exemple, un objet de 2 tuples (ou paire) a deux éléments.</span><span class="sxs-lookup"><span data-stu-id="60296-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="60296-107">La première peut être un `Boolean` valeur, alors que le second est un `String`.</span><span class="sxs-lookup"><span data-stu-id="60296-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="60296-108">Étant donné que les tuples permettent de stocker plusieurs valeurs dans un objet unique, ils sont souvent utilisés comme une solution légère pour retourner plusieurs valeurs à partir d’une méthode.</span><span class="sxs-lookup"><span data-stu-id="60296-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="60296-109">Prise en charge de tuple nécessite le <xref:System.ValueTuple> type.</span><span class="sxs-lookup"><span data-stu-id="60296-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="60296-110">Si le 4.7 Framework .NET n’est pas installé, vous devez ajouter le package NuGet `System.ValueTuple`, qui est disponible sur la galerie NuGet.</span><span class="sxs-lookup"><span data-stu-id="60296-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="60296-111">Sans ce package, vous pouvez obtenir une erreur de compilation semblable à « Type prédéfini 'ValueTuple(Of,,,)' n’est pas défini ou importé. »</span><span class="sxs-lookup"><span data-stu-id="60296-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="60296-112">Instanciation et l’utilisation d’un tuple</span><span class="sxs-lookup"><span data-stu-id="60296-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="60296-113">Vous instanciez un tuple en mettant ses parenthèses de messagerie instantanée de valeurs délimitées par des virgules.</span><span class="sxs-lookup"><span data-stu-id="60296-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="60296-114">Chacune de ces valeurs devient alors un champ du tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="60296-115">Par exemple, le code suivant définit une triple (ou un objet de 3 tuples) avec un `Date` comme première valeur, un `String` en tant que le second et un `Boolean` en tant que son troisième.</span><span class="sxs-lookup"><span data-stu-id="60296-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="60296-116">Par défaut, le nom de chaque champ dans un tuple se compose de la chaîne `Item` , ainsi que la basée sur une position dans le tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="60296-117">Pour cet objet de 3 tuples, les `Date` champ est `Item1`, le `String` champ est `Item2`et le `Boolean` champ est `Item3`.</span><span class="sxs-lookup"><span data-stu-id="60296-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="60296-118">L’exemple suivant affiche les valeurs des champs du tuple instanciée dans la ligne de code précédente</span><span class="sxs-lookup"><span data-stu-id="60296-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="60296-119">Les champs d’un tuple Visual Basic sont en lecture-écriture ; une fois que vous avez instancié un tuple, vous pouvez modifier ses valeurs.</span><span class="sxs-lookup"><span data-stu-id="60296-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="60296-120">L’exemple suivant modifie deux des trois champs de tuple créés dans l’exemple précédent et affiche le résultat.</span><span class="sxs-lookup"><span data-stu-id="60296-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="60296-121">Instanciation et l’utilisation d’un tuple nommé</span><span class="sxs-lookup"><span data-stu-id="60296-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="60296-122">Au lieu d’utiliser des noms par défaut pour les champs d’un tuple, vous pouvez instancier un *nommé tuple* en assignant vos propres noms pour les éléments du tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="60296-123">Les champs du tuple sont ensuite accessible par leurs noms attribués *ou* par leurs noms par défaut.</span><span class="sxs-lookup"><span data-stu-id="60296-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="60296-124">L’exemple suivant instancie le tuple de 3 même comme précédemment, à ceci près qu’il nomme explicitement le premier champ `EventDate`, le deuxième `Name`et la troisième `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="60296-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="60296-125">Il affiche les valeurs de champ, les modifie et affiche les valeurs de champ à nouveau.</span><span class="sxs-lookup"><span data-stu-id="60296-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="tuples-versus-structures"></a><span data-ttu-id="60296-126">Tuples et structures</span><span class="sxs-lookup"><span data-stu-id="60296-126">Tuples versus structures</span></span>

<span data-ttu-id="60296-127">Un tuple de Visual Basic est un type valeur qui est une instance de l’un de l’un **System.ValueTuple** types génériques.</span><span class="sxs-lookup"><span data-stu-id="60296-127">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="60296-128">Par exemple, le `holiday` tuple défini dans l’exemple précédent est une instance de la <xref:System.ValueTuple%603> structure.</span><span class="sxs-lookup"><span data-stu-id="60296-128">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="60296-129">Il est conçu pour être un conteneur léger pour les données.</span><span class="sxs-lookup"><span data-stu-id="60296-129">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="60296-130">Étant donné que le tuple vise pour faciliter la création d’un objet avec plusieurs éléments de données, il ne dispose pas de certaines fonctionnalités susceptibles de présenter une structure personnalisée.</span><span class="sxs-lookup"><span data-stu-id="60296-130">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="60296-131">Elles incluent notamment :</span><span class="sxs-lookup"><span data-stu-id="60296-131">These include:</span></span>

- <span data-ttu-id="60296-132">Les membres des clients.</span><span class="sxs-lookup"><span data-stu-id="60296-132">Customer members.</span></span> <span data-ttu-id="60296-133">Vous ne pouvez pas définir vos propres propriétés, méthodes ou événements pour un tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-133">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="60296-134">Validation.</span><span class="sxs-lookup"><span data-stu-id="60296-134">Validation.</span></span> <span data-ttu-id="60296-135">Impossible de valider les données assignées à des champs.</span><span class="sxs-lookup"><span data-stu-id="60296-135">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="60296-136">Immuabilité.</span><span class="sxs-lookup"><span data-stu-id="60296-136">Immutability.</span></span> <span data-ttu-id="60296-137">Les tuples Visual Basic sont mutables.</span><span class="sxs-lookup"><span data-stu-id="60296-137">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="60296-138">En revanche, une structure personnalisée permet de vous contrôler si une instance est mutable ou immuable.</span><span class="sxs-lookup"><span data-stu-id="60296-138">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="60296-139">Si des membres personnalisés, la propriété et la validation de champ ou que l’immuabilité est importante, vous devez utiliser Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) instruction afin de définir un type de valeur personnalisée.</span><span class="sxs-lookup"><span data-stu-id="60296-139">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="60296-140">Un tuple de Visual Basic n’hérite pas les membres de sa **ValueTuple** type.</span><span class="sxs-lookup"><span data-stu-id="60296-140">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="60296-141">En plus de ses champs, notamment les méthodes suivantes :</span><span class="sxs-lookup"><span data-stu-id="60296-141">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="60296-142">Membre</span><span class="sxs-lookup"><span data-stu-id="60296-142">Member</span></span> | <span data-ttu-id="60296-143">Description</span><span class="sxs-lookup"><span data-stu-id="60296-143">Description</span></span> |
| ---|---|
| <span data-ttu-id="60296-144">CompareTo</span><span class="sxs-lookup"><span data-stu-id="60296-144">CompareTo</span></span> | <span data-ttu-id="60296-145">Compare le tuple actif à un autre tuple avec le même nombre d’éléments.</span><span class="sxs-lookup"><span data-stu-id="60296-145">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="60296-146">Equals</span><span class="sxs-lookup"><span data-stu-id="60296-146">Equals</span></span> | <span data-ttu-id="60296-147">Détermine si le tuple actif est égal à un autre objet ou tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-147">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="60296-148">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="60296-148">GetHashCode</span></span> | <span data-ttu-id="60296-149">Calcule le code de hachage pour l’instance actuelle.</span><span class="sxs-lookup"><span data-stu-id="60296-149">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="60296-150">ToString</span><span class="sxs-lookup"><span data-stu-id="60296-150">ToString</span></span> | <span data-ttu-id="60296-151">Retourne la représentation sous forme de chaîne de ce tuple, qui prend la forme `(Item1, Item2...)`, où `Item1` et `Item2` représentent les valeurs des champs du tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-151">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="60296-152">En outre, le **ValueTuple** types implémentent <xref:System.Collections.IStructuralComparable> et <xref:System.Collections.IStructuralEquatable> interfaces, ce qui vous permet de définir des comparateurs de client.</span><span class="sxs-lookup"><span data-stu-id="60296-152">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="60296-153">Affectation et tuples</span><span class="sxs-lookup"><span data-stu-id="60296-153">Assignment and tuples</span></span>

<span data-ttu-id="60296-154">Visual Basic prend en charge l’assignation entre les types de tuple qui ont le même nombre de champs.</span><span class="sxs-lookup"><span data-stu-id="60296-154">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="60296-155">Les types de champs peuvent être converties si une des conditions suivantes est remplie :</span><span class="sxs-lookup"><span data-stu-id="60296-155">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="60296-156">Le champ source et cible sont du même type.</span><span class="sxs-lookup"><span data-stu-id="60296-156">The source and target field are of the same type.</span></span>

- <span data-ttu-id="60296-157">Une conversion étendue (ou implicite) du type de source pour le type de cible est définie.</span><span class="sxs-lookup"><span data-stu-id="60296-157">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="60296-158">`Option Strict`est `On`, et une conversion restrictive (ou explicite) du type de source pour le type de cible est définie.</span><span class="sxs-lookup"><span data-stu-id="60296-158">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="60296-159">Cette conversion peut lever une exception si la valeur source est en dehors de la plage du type cible.</span><span class="sxs-lookup"><span data-stu-id="60296-159">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="60296-160">Les autres conversions ne sont pas prises en compte pour les affectations.</span><span class="sxs-lookup"><span data-stu-id="60296-160">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="60296-161">Examinons les types d’affectation qui sont autorisés entre les types tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-161">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="60296-162">Prenez en compte les variables utilisées dans les exemples suivants :</span><span class="sxs-lookup"><span data-stu-id="60296-162">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="60296-163">Les deux premières variables `unnamed` et `anonymous`, n’ont pas de noms sémantiques fournies pour les champs.</span><span class="sxs-lookup"><span data-stu-id="60296-163">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="60296-164">Leurs noms de champ sont la valeur par défaut `Item1` et `Item2`.</span><span class="sxs-lookup"><span data-stu-id="60296-164">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="60296-165">Les deux dernières variables `named` et `differentName` ont des noms de champ de sémantique.</span><span class="sxs-lookup"><span data-stu-id="60296-165">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="60296-166">Notez que ces deux tuples ont des noms différents pour les champs.</span><span class="sxs-lookup"><span data-stu-id="60296-166">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="60296-167">Les quatre de ces tuples ont le même nombre de champs (également appelé « arité ») et les types de ces champs sont identiques.</span><span class="sxs-lookup"><span data-stu-id="60296-167">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="60296-168">Par conséquent, toutes ces affectations fonctionnent :</span><span class="sxs-lookup"><span data-stu-id="60296-168">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="60296-169">Notez que les noms des tuples ne sont pas affectés.</span><span class="sxs-lookup"><span data-stu-id="60296-169">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="60296-170">Les valeurs des champs sont affectées suivant l’ordre des champs dans le tuple.</span><span class="sxs-lookup"><span data-stu-id="60296-170">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="60296-171">Enfin, notez que nous pouvons attribuer le `named` tuple à la `conversion` tuple, même si le premier champ de `named` est un `Integer`et le premier champ de `conversion` est un `Long`.</span><span class="sxs-lookup"><span data-stu-id="60296-171">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="60296-172">Cette attribution a réussi, car la conversion d’un `Integer` à un `Long` est une conversion étendue.</span><span class="sxs-lookup"><span data-stu-id="60296-172">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="60296-173">Tuples avec différents nombres de champs ne sont pas être assignés :</span><span class="sxs-lookup"><span data-stu-id="60296-173">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="60296-174">Tuples comme valeurs de retour de méthode</span><span class="sxs-lookup"><span data-stu-id="60296-174">Tuples as method return values</span></span>

<span data-ttu-id="60296-175">Une méthode peut retourner une seule valeur.</span><span class="sxs-lookup"><span data-stu-id="60296-175">A method can return only a single value.</span></span> <span data-ttu-id="60296-176">Souvent, cependant, vous aimeriez un appel de méthode pour retourner plusieurs valeurs.</span><span class="sxs-lookup"><span data-stu-id="60296-176">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="60296-177">Il existe plusieurs façons de contourner cette limitation :</span><span class="sxs-lookup"><span data-stu-id="60296-177">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="60296-178">Vous pouvez créer une classe personnalisée ou une structure dont les propriétés ou champs représentent les valeurs retournées par la méthode.</span><span class="sxs-lookup"><span data-stu-id="60296-178">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="60296-179">Par conséquent, est une solution de haute densité. Il requiert que vous définissez un type personnalisé dont seul objectif consiste à récupérer les valeurs à partir d’un appel de méthode.</span><span class="sxs-lookup"><span data-stu-id="60296-179">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="60296-180">Vous pouvez retourner une valeur unique à partir de la méthode et renvoyer les valeurs restantes en les passant par référence à la méthode.</span><span class="sxs-lookup"><span data-stu-id="60296-180">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="60296-181">Cela implique la surcharge de l’instanciation d’une variable et les risques par inadvertance en remplaçant la valeur de la variable que vous passez par référence.</span><span class="sxs-lookup"><span data-stu-id="60296-181">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="60296-182">Vous pouvez utiliser un tuple, qui fournit une solution légère pour la récupération de plusieurs valeurs de retour.</span><span class="sxs-lookup"><span data-stu-id="60296-182">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="60296-183">Par exemple, le **TryParse** méthodes de retour de .NET un `Boolean` valeur qui indique si l’opération d’analyse a réussi.</span><span class="sxs-lookup"><span data-stu-id="60296-183">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="60296-184">Le résultat de l’opération d’analyse est retourné dans une variable passée par référence à la méthode.</span><span class="sxs-lookup"><span data-stu-id="60296-184">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="60296-185">Normalement, un appel à l’une méthode d’analyse comme <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> présente l’aspect suivant :</span><span class="sxs-lookup"><span data-stu-id="60296-185">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="60296-186">Nous pouvons retourner un tuple à partir de l’opération d’analyse, si nous encapsulent l’appel à la <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> méthode dans votre propre méthode.</span><span class="sxs-lookup"><span data-stu-id="60296-186">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="60296-187">Dans l’exemple suivant, `NumericLibrary.ParseInteger` appelle la <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> méthode et retourne un tuple nommé avec deux éléments.</span><span class="sxs-lookup"><span data-stu-id="60296-187">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="60296-188">Vous pouvez ensuite appeler la méthode avec un code semblable au suivant :</span><span class="sxs-lookup"><span data-stu-id="60296-188">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="60296-189">Les tuples de Visual Basic et les tuples dans le .NET Framework</span><span class="sxs-lookup"><span data-stu-id="60296-189">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="60296-190">Un tuple de Visual Basic est une instance de l’un de le **System.ValueTuple** des types génériques qui ont été introduits dans le 4.7 Framework .NET.</span><span class="sxs-lookup"><span data-stu-id="60296-190">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="60296-191">Le .NET Framework inclut également un ensemble de génériques **System.Tuple** classes.</span><span class="sxs-lookup"><span data-stu-id="60296-191">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="60296-192">Ces classes, toutefois, diffèrent des tuples de Visual Basic et le **System.ValueTuple** des types génériques dans de plusieurs façons :</span><span class="sxs-lookup"><span data-stu-id="60296-192">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="60296-193">Les éléments de la **Tuple** les classes sont des propriétés nommées `Item1`, `Item2`, et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="60296-193">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="60296-194">Dans Visual Basic tuples et **ValueTuple** types d’éléments de tuple, sont des champs.</span><span class="sxs-lookup"><span data-stu-id="60296-194">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="60296-195">Vous ne pouvez attribuer des noms explicites pour les éléments d’un **Tuple** instance ou d’un **ValueTuple** instance.</span><span class="sxs-lookup"><span data-stu-id="60296-195">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="60296-196">Visual Basic vous permet d’attribuer des noms qui communiquent la signification des champs.</span><span class="sxs-lookup"><span data-stu-id="60296-196">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="60296-197">Les propriétés d’un **Tuple** instance sont en lecture seule ; les tuples sont immuables.</span><span class="sxs-lookup"><span data-stu-id="60296-197">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="60296-198">Dans Visual Basic tuples et **ValueTuple** , types de champs de tuple sont en lecture-écriture ; les tuples sont mutables.</span><span class="sxs-lookup"><span data-stu-id="60296-198">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="60296-199">Le type générique **Tuple** types sont des types référence.</span><span class="sxs-lookup"><span data-stu-id="60296-199">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="60296-200">L’utilisation de ces **Tuple** types signifie que de l’allocation d’objets.</span><span class="sxs-lookup"><span data-stu-id="60296-200">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="60296-201">Sur des chemins réactifs, cela peut avoir un impact mesurable sur les performances de votre application.</span><span class="sxs-lookup"><span data-stu-id="60296-201">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="60296-202">Visual Basic tuples et **ValueTuple** types sont des types valeur.</span><span class="sxs-lookup"><span data-stu-id="60296-202">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="60296-203">Méthodes d’extension dans le <xref:System.TupleExtensions> classe facilitent la conversion entre des tuples de Visual Basic et .NET **Tuple** objets.</span><span class="sxs-lookup"><span data-stu-id="60296-203">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="60296-204">Le **ToTuple** méthode convertit un tuple de Visual Basic .NET **Tuple** objet et le **ToValueTuple** méthode convertit un .NET **Tuple** objet à un tuple de Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="60296-204">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="60296-205">L’exemple suivant crée un tuple, le convertit en un .NET **Tuple** objet et convertit le retour à un tuple de Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="60296-205">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="60296-206">L’exemple compare ensuite ce tuple avec celui d’origine pour vous assurer qu’ils sont égaux.</span><span class="sxs-lookup"><span data-stu-id="60296-206">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="60296-207">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="60296-207">See also</span></span>

[<span data-ttu-id="60296-208">Informations de référence sur le langage Visual Basic</span><span class="sxs-lookup"><span data-stu-id="60296-208">Visual Basic Language Reference</span></span>](index.md)  
