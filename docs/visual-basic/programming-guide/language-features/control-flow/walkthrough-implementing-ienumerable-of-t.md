---
title: "Implémentation d’IEnumerable dans Visual Basic"
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
caps.latest.revision: "15"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 45b4008f0bf3ae0f303aa029e7bff5b82ab6f259
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/21/2017
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="27e4c-102">Procédure pas à pas : implémentation d'IEnumerable(Of T) en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="27e4c-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="27e4c-103">Le <xref:System.Collections.Generic.IEnumerable%601> interface est implémentée par les classes qui peuvent retourner une séquence de valeurs élément par élément à la fois.</span><span class="sxs-lookup"><span data-stu-id="27e4c-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="27e4c-104">L’avantage de retourner des données d’un élément à la fois est que vous n’avez pas à charger l’ensemble complet des données en mémoire pour l’utiliser.</span><span class="sxs-lookup"><span data-stu-id="27e4c-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="27e4c-105">Vous devez uniquement utiliser suffisamment de mémoire pour le chargement d’un élément unique à partir des données.</span><span class="sxs-lookup"><span data-stu-id="27e4c-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="27e4c-106">Classes qui implémentent la `IEnumerable(T)` interface peut être utilisée avec `For Each` boucles ou les requêtes LINQ.</span><span class="sxs-lookup"><span data-stu-id="27e4c-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="27e4c-107">Par exemple, considérez une application qui doit lire un fichier texte volumineux et retourner chaque ligne du fichier qui correspond aux critères de recherche spécifique.</span><span class="sxs-lookup"><span data-stu-id="27e4c-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="27e4c-108">L’application utilise une requête LINQ pour retourner des lignes à partir du fichier qui correspondent aux critères spécifiés.</span><span class="sxs-lookup"><span data-stu-id="27e4c-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="27e4c-109">Pour interroger le contenu du fichier à l’aide d’une requête LINQ, l’application pourrait charger le contenu du fichier dans un tableau ou une collection.</span><span class="sxs-lookup"><span data-stu-id="27e4c-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="27e4c-110">Toutefois, le chargement de la totalité du fichier dans un tableau ou une collection consomme beaucoup plus de mémoire que nécessaire.</span><span class="sxs-lookup"><span data-stu-id="27e4c-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="27e4c-111">La requête LINQ pourrait à la place interroger le contenu du fichier en utilisant une classe énumérable, en retournant uniquement les valeurs qui correspondent aux critères de recherche.</span><span class="sxs-lookup"><span data-stu-id="27e4c-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="27e4c-112">Requêtes qui retournent uniquement quelques valeurs correspondantes consomme beaucoup moins de mémoire.</span><span class="sxs-lookup"><span data-stu-id="27e4c-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="27e4c-113">Vous pouvez créer une classe qui implémente le <xref:System.Collections.Generic.IEnumerable%601> interface à exposer les données source sous forme de données énumérables.</span><span class="sxs-lookup"><span data-stu-id="27e4c-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="27e4c-114">Votre classe qui implémente le `IEnumerable(T)` interface nécessitera une autre classe qui implémente le <xref:System.Collections.Generic.IEnumerator%601> interface à une itération au sein de la source de données.</span><span class="sxs-lookup"><span data-stu-id="27e4c-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="27e4c-115">Ces deux classes permettent de retourner les éléments de données séquentiellement comme un type spécifique.</span><span class="sxs-lookup"><span data-stu-id="27e4c-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="27e4c-116">Dans cette procédure pas à pas, vous allez créer une classe qui implémente le `IEnumerable(Of String)` interface et une classe qui implémente le `IEnumerator(Of String)` interface pour lire une fichier texte une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="27e4c-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="27e4c-117">Création de la classe énumérable</span><span class="sxs-lookup"><span data-stu-id="27e4c-117">Creating the Enumerable Class</span></span>  
  
|<span data-ttu-id="27e4c-118">Pour créer le projet de classe énumérable</span><span class="sxs-lookup"><span data-stu-id="27e4c-118">To create the enumerable class project</span></span>|  
|---|  
|<span data-ttu-id="27e4c-119">1.  Dans [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)], dans le menu **Fichier**, pointez sur **Nouveau**, puis cliquez sur **Projet**.</span><span class="sxs-lookup"><span data-stu-id="27e4c-119">1.  In [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)], on the **File** menu, point to **New** and then click **Project**.</span></span><br /><span data-ttu-id="27e4c-120">2.  Dans la boîte de dialogue **Nouveau projet**, dans le volet **Types de projets**, vérifiez que **Windows** est sélectionné.</span><span class="sxs-lookup"><span data-stu-id="27e4c-120">2.  In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="27e4c-121">Sélectionnez **Bibliothèque de classes** dans le volet **Modèles**.</span><span class="sxs-lookup"><span data-stu-id="27e4c-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="27e4c-122">Dans la zone **Nom**, tapez `StreamReaderEnumerable`, puis cliquez sur **OK**.</span><span class="sxs-lookup"><span data-stu-id="27e4c-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="27e4c-123">Le nouveau projet s’affiche.</span><span class="sxs-lookup"><span data-stu-id="27e4c-123">The new project is displayed.</span></span><br /><span data-ttu-id="27e4c-124">3.  Dans **l’Explorateur de solutions**, cliquez sur le fichier Class1.vb et cliquez sur **renommer**.</span><span class="sxs-lookup"><span data-stu-id="27e4c-124">3.  In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="27e4c-125">Renommez le fichier `StreamReaderEnumerable.vb` et appuyez sur Entrée.</span><span class="sxs-lookup"><span data-stu-id="27e4c-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="27e4c-126">Quand vous renommez le fichier, la classe est également renommée `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="27e4c-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="27e4c-127">Cette classe va implémenter l’interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="27e4c-127">This class will implement the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="27e4c-128">4.  Cliquez sur le projet StreamReaderEnumerable, pointez sur **ajouter**, puis cliquez sur **un nouvel élément**.</span><span class="sxs-lookup"><span data-stu-id="27e4c-128">4.  Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="27e4c-129">Sélectionnez le **classe** modèle.</span><span class="sxs-lookup"><span data-stu-id="27e4c-129">Select the **Class** template.</span></span> <span data-ttu-id="27e4c-130">Dans le **nom** , tapez `StreamReaderEnumerator.vb` et cliquez sur **OK**.</span><span class="sxs-lookup"><span data-stu-id="27e4c-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>|  
  
 <span data-ttu-id="27e4c-131">La première classe dans ce projet est la classe énumérable et implémentera le `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="27e4c-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="27e4c-132">Cette interface générique implémente la <xref:System.Collections.IEnumerable> interface et garantit que les consommateurs de cette classe peuvent accéder aux valeurs de type `String`.</span><span class="sxs-lookup"><span data-stu-id="27e4c-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
|<span data-ttu-id="27e4c-133">Pour ajouter le code pour implémenter IEnumerable</span><span class="sxs-lookup"><span data-stu-id="27e4c-133">To add the code to implement IEnumerable</span></span>|  
|---|  
|<span data-ttu-id="27e4c-134">1.  Ouvrez le fichier StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="27e4c-134">1.  Open the StreamReaderEnumerable.vb file.</span></span><br /><span data-ttu-id="27e4c-135">2.  Sur la ligne après `Public Class StreamReaderEnumerable`, tapez la commande suivante et appuyez sur ENTRÉE.</span><span class="sxs-lookup"><span data-stu-id="27e4c-135">2.  On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#1](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_1.vb)]<br />     [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)]<span data-ttu-id="27e4c-136">remplit automatiquement la classe avec les membres qui sont requis par le `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="27e4c-136"> automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="27e4c-137">3.  Cette classe énumérable lira les lignes à partir d’une fichier texte une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="27e4c-137">3.  This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="27e4c-138">Ajoutez le code suivant à la classe pour exposer un constructeur public qui accepte un chemin d’accès de fichier comme paramètre d’entrée.</span><span class="sxs-lookup"><span data-stu-id="27e4c-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#2](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_2.vb)]<br /><span data-ttu-id="27e4c-139">4.  Votre implémentation de la <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> méthode de la `IEnumerable(Of String)` interface retourne une nouvelle instance de la `StreamReaderEnumerator` classe.</span><span class="sxs-lookup"><span data-stu-id="27e4c-139">4.  Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="27e4c-140">L’implémentation de la `GetEnumerator` méthode de la `IEnumerable` interface peut être effectuée `Private`, car vous avez exposer uniquement les membres de le `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="27e4c-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="27e4c-141">Remplacez le code qui [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] généré pour le `GetEnumerator` méthodes avec le code suivant.</span><span class="sxs-lookup"><span data-stu-id="27e4c-141">Replace the code that [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] generated for the `GetEnumerator` methods with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#3](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_3.vb)]|  
  
|<span data-ttu-id="27e4c-142">Pour ajouter le code pour implémenter IEnumerator</span><span class="sxs-lookup"><span data-stu-id="27e4c-142">To add the code to implement IEnumerator</span></span>|  
|---|  
|<span data-ttu-id="27e4c-143">1.  Ouvrez le fichier StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="27e4c-143">1.  Open the StreamReaderEnumerator.vb file.</span></span><br /><span data-ttu-id="27e4c-144">2.  Sur la ligne après `Public Class StreamReaderEnumerator`, tapez la commande suivante et appuyez sur ENTRÉE.</span><span class="sxs-lookup"><span data-stu-id="27e4c-144">2.  On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#4](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_4.vb)]<br />     [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)]<span data-ttu-id="27e4c-145">remplit automatiquement la classe avec les membres qui sont requis par le `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="27e4c-145"> automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span><br /><span data-ttu-id="27e4c-146">3.  La classe enumerator ouvre le fichier texte et exécute le fichier d’e/s pour lire les lignes à partir du fichier.</span><span class="sxs-lookup"><span data-stu-id="27e4c-146">3.  The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="27e4c-147">Ajoutez le code suivant à la classe pour exposer un constructeur public qui accepte un chemin d’accès de fichier comme paramètre d’entrée et ouvrez le fichier texte pour la lecture.</span><span class="sxs-lookup"><span data-stu-id="27e4c-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#5](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_5.vb)]<br /><span data-ttu-id="27e4c-148">4.  Le `Current` propriétés à la fois pour le `IEnumerator(Of String)` et `IEnumerator` interfaces retournent l’élément actuel à partir du fichier texte comme un `String`.</span><span class="sxs-lookup"><span data-stu-id="27e4c-148">4.  The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="27e4c-149">L’implémentation de la `Current` propriété de la `IEnumerator` interface peut être effectuée `Private`, car vous avez exposer uniquement les membres de le `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="27e4c-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="27e4c-150">Remplacez le code qui [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] généré pour le `Current` propriétés avec le code suivant.</span><span class="sxs-lookup"><span data-stu-id="27e4c-150">Replace the code that [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] generated for the `Current` properties with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#6](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_6.vb)]<br /><span data-ttu-id="27e4c-151">5.  Le `MoveNext` méthode de la `IEnumerator` interface accède à l’élément suivant dans le fichier texte et met à jour la valeur retournée par la `Current` propriété.</span><span class="sxs-lookup"><span data-stu-id="27e4c-151">5.  The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="27e4c-152">S’il en existe aucun élément à lire, le `MoveNext` méthode retourne `False`; sinon la `MoveNext` méthode renvoie `True`.</span><span class="sxs-lookup"><span data-stu-id="27e4c-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="27e4c-153">Ajoutez le code suivant à la méthode `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="27e4c-153">Add the following code to the `MoveNext` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#7](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_7.vb)]<br /><span data-ttu-id="27e4c-154">6.  Le `Reset` méthode de la `IEnumerator` interface ordonne à l’itérateur pointe vers le début du fichier texte et efface la valeur de l’élément actuel.</span><span class="sxs-lookup"><span data-stu-id="27e4c-154">6.  The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="27e4c-155">Ajoutez le code suivant à la méthode `Reset` .</span><span class="sxs-lookup"><span data-stu-id="27e4c-155">Add the following code to the `Reset` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#8](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_8.vb)]<br /><span data-ttu-id="27e4c-156">7.  Le `Dispose` méthode de la `IEnumerator` interface garantit que toutes les ressources non managées sont libérées avant la destruction de l’itérateur.</span><span class="sxs-lookup"><span data-stu-id="27e4c-156">7.  The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="27e4c-157">Le handle de fichier est utilisé par le `StreamReader` objet est une ressource non managée et doit être fermé avant la destruction de l’instance de l’itérateur.</span><span class="sxs-lookup"><span data-stu-id="27e4c-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="27e4c-158">Remplacez le code qui [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] généré pour le `Dispose` méthode avec le code suivant.</span><span class="sxs-lookup"><span data-stu-id="27e4c-158">Replace the code that [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] generated for the `Dispose` method with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#9](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_9.vb)]|  
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="27e4c-159">À l’aide de l’itérateur d’exemple</span><span class="sxs-lookup"><span data-stu-id="27e4c-159">Using the Sample Iterator</span></span>  
 <span data-ttu-id="27e4c-160">Vous pouvez utiliser une classe énumérable dans votre code avec les structures de contrôle qui nécessitent un objet qui implémente `IEnumerable`, comme un `For Next` boucle ou une requête LINQ.</span><span class="sxs-lookup"><span data-stu-id="27e4c-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="27e4c-161">L’exemple suivant montre le `StreamReaderEnumerable` dans une requête LINQ.</span><span class="sxs-lookup"><span data-stu-id="27e4c-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_10.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="27e4c-162">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="27e4c-162">See Also</span></span>  
 [<span data-ttu-id="27e4c-163">Introduction à LINQ en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="27e4c-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)  
 [<span data-ttu-id="27e4c-164">Flux de contrôle</span><span class="sxs-lookup"><span data-stu-id="27e4c-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)  
 [<span data-ttu-id="27e4c-165">Structures de boucle</span><span class="sxs-lookup"><span data-stu-id="27e4c-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)  
 [<span data-ttu-id="27e4c-166">For Each...Next (instruction)</span><span class="sxs-lookup"><span data-stu-id="27e4c-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
